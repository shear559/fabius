{
  "name": "fabius-runtime",
  "version": "3.0.1",
  "private": true,
  "description": "Repo-local runtime component for fabius. It is not a standalone npm CLI: run it from a full fabius checkout so sibling skills/ and provenance/ are present.",
  "type": "module",
  "files": [
    "README.md",
    "fabius.mjs",
    "src/**/*.mjs",
    "test/**/*.test.mjs",
    "tools/fetch-vectors.sh"
  ],
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "vectors": "bash tools/fetch-vectors.sh",
    "doctor": "node fabius.mjs doctor"
  },
  "dependencies": {},
  "license": "UNLICENSED"
}
