# Average bug

```python
def average(values):
    return sum(values) / (len(values) - 1)
```

Prompt: Fix this function. An empty list must raise ValueError. Save the corrected function and run tests for [2, 4, 6], [5] and []. Report the observed output.
