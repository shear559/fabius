# Fabius Cohors — the agent-framework & tool-caller map

Loaded on demand by `fabius-cohors`. The skill owns the *definition schema* and the five orchestration patterns; this is the current **best-in-class stack** (2026) to build them on — the frameworks that define/orchestrate agents, the eval that picks the tool-caller, and the open **models** to put behind the action layer. One license law dominates the model half: **a strong open tool-caller's HF tag often lies.** Most (Hermes, Functionary, watt-tool, xLAM-70B) are **Llama fine-tunes** — the *Llama Community License* (attribution "Built with Llama", 700M-MAU clause, no training-other-models) governs the weights regardless of an "Apache/MIT" tag on the repo. For a sellable product, prefer genuinely permissive backbones.

## Frameworks — pick by orchestration pattern

| Framework | License | Maps to cohors pattern · reach for it when |
|---|---|---|
| **LangGraph** | MIT | Graph/state-machine with durable checkpoints + HITL — explicit state, branching, resumable multi-step. (LangSmith obs is separate paid SaaS.) |
| **OpenAI Agents SDK** | MIT | **Swarm / handoff** — minimal provider-agnostic loop (100+ LLMs), handoffs + agent-as-tool + guardrails + tracing. TS port exists. Successor to "Swarm". |
| **Claude Agent SDK** | MIT *code* · ⚠️ **Anthropic Commercial Terms** for *use* | The **definition schema + permission model** directly — built-in tools, hooks, subagents, custom tools via in-process MCP, sessions, and the same skills/commands/memory loading as Claude Code, in Python or TypeScript. **The CLI ships inside the package** — most installs need nothing else; point `pathToClaudeCodeExecutable` / `cli_path` at a system install only when the platform wheel or the npm optional dependency was skipped. Any other language drives the same loop by running the CLI headless (`-p --output-format json`). *Read the licence line twice: the repo LICENSE is MIT, but **use** of the SDK is governed by Anthropic's Commercial Terms, and the branding rules forbid shipping anything called "Claude Code" or offering claude.ai login to your own users.* |
| **CrewAI** | MIT | **Sequential / hierarchical** role-assignment — "Crews" of role-playing specialists + "Flows". (AMP/Enterprise is paid.) |
| **MS Agent Framework** | MIT | No longer just the AutoGen+Semantic-Kernel successor — its **harness factory** turns a bare chat client into a durable agent: history, compaction, todos, modes, file memory, skills, approvals, background agents, bounded loops, OTel wired in (→ *Harness anatomy*, below). *Classic `microsoft/autogen` is maintenance-mode;* **AG2** (Apache-2.0) is the community fork keeping the old conversable-agent API. |
| **Pydantic-AI** | MIT | The **typed tool contract** — Pydantic models validate every tool arg + structured output, cutting agent failure modes. |
| **Google ADK** | Apache-2.0 | **Hierarchical** (manager delegates to sub-agents) with first-class **eval** + A2A delegation (→ *Crossing an organizational boundary*, below). |
| **Mastra** | Apache-2.0 (core) | The **TypeScript-native** option — agents+tools+memory+workflows in Node/edge (Vercel/Workers). *`ee/` is a proprietary Enterprise license.* |
| **smolagents** (HF) | Apache-2.0 | The **lean single-agent** shape — "code agents" write actions as executable Python (fewer steps than JSON calls). *Code-as-action = arbitrary execution → MUST sandbox (E2B/Modal/Docker).* |
| **Flowise** | ⚠️ Apache-2.0 **open-core** | The **visual / no-code canvas for building the *agent itself*** — this is where a "drag-and-drop / no-code AI-agent builder" request belongs (cohors, **not** machina's n8n: the deliverable here is a *generative agent*, not a fixed workflow — the artifact decides). A node canvas wires LLMs, tools, RAG, and control flow into a flow, then exposes it as a **REST endpoint + exportable JSON + embeddable widget**. Prototype the *shape* of the five patterns visually, then hand-code the production agent in the cohors schema. *Community core is genuinely Apache-2.0; the enterprise partition (SSO/RBAC/workspaces, `packages/server/src/enterprise`) is a proprietary Commercial License — don't activate or redistribute it. Heavyweight self-hosted service (Node+React+DB), not an embeddable library.* |
| **DeepSeek-harness** (`dsh`) | MIT | **Plugin service-container** — models, tools, memory, UI, and adapters are all hot-reloadable plugins over a DI container (**Cordis**) with a lifecycle; composition is runtime-reconfigurable, not assembled-once. Reach for it over MS-AF's *fixed-order factory* when the capability set must stay open-ended and swappable at runtime — vs a known set assembled in one load-bearing order (→ *Two harness philosophies*, below). ⚠️ Developer preview (`master`) — pin before citing. |

## Harness anatomy — what a complete agent factory assembles

The harness-factory shape above is worth memorizing as a checklist, because the **assembly order is the checklist**: the history provider comes FIRST — every provider registered after it must see the loaded messages — then compaction, todo, mode, and memory, then the opt-in capabilities, then whatever the user adds. Middleware ordering is equally load-bearing and inverts the intuition: **loop middleware sits outermost** (each iteration is a full agent run, approvals included), **tool-approval next** (its re-invocation loop re-runs everything inside it), **message-injection innermost**. Get either ordering wrong and the failure is silent — a provider that never saw history, an injected message the approval loop duplicates.

- **Safe-defaults posture.** Benign capabilities ship on-by-default with a disable flag each; the risky ones — shell, shared file access, background agents, unbounded loops — are strictly opt-in and loudly marked experimental. Copy the posture, not just the feature list.
- **The toolset is composed per run, never static.** Capabilities register their tools per-invocation on the session — which is what lets a mode or a permission change actually change what the model can reach on the next run.
- **Compaction fails silent.** Omitting the token-budget parameters silently disables compaction — declare both the context window and the output ceiling, or state explicitly why compaction is off. An agent that "has compaction" but was never given a budget has nothing.
- **Patterns only** — carry the anatomy, never this framework's API names: several of its surfaces are still experimental, and the checklist transfers to any harness while the names do not.

## Two harness philosophies — fixed-order factory vs plugin service-container

*Harness anatomy* above described one way to build an agent factory: MS Agent Framework's **fixed-order factory** — a known set of capabilities (history → compaction → todo → mode → memory → opt-ins) assembled in one load-bearing sequence, where the assembly order *is* the correctness. There is an opposite philosophy, and choosing between them is a real design decision, not a detail.

- **DeepSeek-harness** (`deepseek-ai/deepseek-harness`, "dsh") · **MIT** · ~139k★ · pushed 2026-08-13 · tagline *"Everything is a Plugin."* — a Node/TS pnpm monorepo (`apps/ packages/ native/ python/`, Vitest/oxlint/tsdown); `npx @deepseek-ai/dsh web` launches a local UI at `127.0.0.1:3080`; plugins carry the `dsh-plugin` tag. It is built on **Cordis**, a service-container / dependency-injection meta-framework with a plugin **lifecycle** and **hot-reload** (Cordis's term: *"spatiotemporal composability"*). *Everything* — models, tools, memory, the UI, adapters — is a plugin over that container. Composition is open-ended and runtime-reconfigurable, not assembled-once.

**The contrast is the choice.** A fixed-order factory buys a *guaranteed* assembly and a checklist you can't get wrong silently — reach for it when the capability set is known and the order is load-bearing (most production agents). A DI service-container buys *swap-at-runtime* — reach for it when the capability set must stay open-ended, when a model or a UI must be hot-swapped without a restart, or when third parties contribute plugins you don't control. The cost is any DI system's cost: the wiring is indirect and "what's actually loaded" is a runtime question, not a read of the assembly code.

**Honest boundary — fabius already lives a form of this.** fabius's own skill/plugin architecture (a router paging specialist layers in on demand) is itself "everything is a plugin," so the *idea* is not the delta. What transfers is narrower and real: the **DI-container + hot-reload lifecycle discipline** as a design reference — a capability *registers → initializes → disposes* against a container and can be replaced live without tearing down its neighbors. Study the lifecycle contract; don't adopt the runtime (a full monorepo + local web UI is the opposite of a lean fabius install).

⚠️ **Version trap.** dsh is a **developer preview** on the `master` branch — DeepSeek states breaking changes are expected, so anything you cite about its plugin API is one force-push from stale. Pin a commit before citing and re-check on bump. `THIRD_PARTY_NOTICES.md` is present — honor it if you ever vendor.

## Crossing an organizational boundary — A2A

The five patterns all assume every worker runs inside a harness *you* own. When the worker is an agent you don't own — another team's, another vendor's — the contract is **A2A**, now **v1.0** under the Linux Foundation (150+ member organizations; shipped inside Azure AI Foundry / Copilot Studio, Bedrock AgentCore Runtime and Google Cloud). Memorize the split, because it settles the routing question outright: **MCP connects an agent to tools and data; A2A connects an agent to another agent across an organizational boundary.**

The wire shape is the cohors contract in someone else's vocabulary, so map onto it rather than inventing a second delegation protocol: an **Agent Card** for discovery (identity, capabilities, skills, service endpoint, auth), a **Task** with an explicit lifecycle (`submitted` → `working` → `input-required` / `completed` / `failed` / `canceled` / `rejected` / `auth-required`), **Messages** for turns and **Artifacts** for results — bound over JSON-RPC, gRPC, or HTTP+JSON/REST.

**A remote agent is still an untrusted worker.** Its Agent Card is a claim, not a credential, and its result crosses a boundary your own reviewer sits behind — validate the return against the output contract *at* that boundary, before it enters the merge (`fabius-praesidium`).

## The deep-research harness — a worked multi-agent shape

When the job is long-horizon research (decompose → gather in parallel → synthesize), don't invent the orchestration cold — there's a canonical worked shape to route against.

- **DeerFlow** (`bytedance/deer-flow`) · **MIT** · ~79k★ · **v2.0.0 cut 25 June 2026**, at the end of a long `v2.0-m*` / `-rc*` tag train — ByteDance's **lead-agent → parallel isolated sub-agents → synthesis** runtime on LangGraph+LangChain, batteries-included: a filesystem abstraction, cross-session memory, a progressively-loaded skill system, sandboxed execution (local/Docker/K8s), MCP tools, and context compaction. The project now bills itself as a **long-horizon "SuperAgent" harness** — sandboxes, memories, tools, skills, subagents, message gateway — not a deep-research pipeline. The reason to keep it: its v1 **coordinator → planner → researcher → reporter** pipeline is the **reference shape** for cohors's *hierarchical + parallel* patterns, and v2's isolated-sub-agent model mirrors the swarm contract (scoped tools + termination per worker). **Adopt as a pattern reference, not a dependency** — it's a full runtime (a large LangChain tree + a Next web UI + IM gateways), the opposite of a lean fabius install. **Version trap, and now it is dated:** anything written before mid-2026 describes v1's narrow deep-research architecture, which remains the reference pipeline but is not what `main` builds today. Pin a version before citing.

## Giving an agent a voice (I/O)

An agent's *voice* is a **modality, not a model**: cohors owns the wiring, `fabius-doctrina` owns the speech model behind it.

- **Voicebox** (`jamiepine/voicebox`) · **MIT** *(app code)* · ~38k★ · v0.5.0 (pre-1.0) — a local-first voice studio (7 TTS engines — Qwen3-TTS, Chatterbox, Kokoro… — plus Whisper dictation and zero-shot cloning) that exposes a **local FastMCP-over-HTTP server**, so any MCP-aware agent (Claude Code, Cursor) gets a chosen **spoken voice** with **no audio leaving the machine**. The clean way to add speech-out to an orchestrated agent — the agent calls a local MCP tool, doctrina's model speaks. **Three caveats fabius prints:** (1) the MIT license covers **Voicebox's code only** — the 7 wrapped model **weights** each carry their own license (several restrict commercial use); verify per-model before shipping. (2) It clones a voice from **seconds of audio with no consent lock** — a biometric-impersonation / deepfake vector: gate it behind consent + provenance (`fabius-praesidium` — the likeness and voice consent gate, [`supply-chain-and-ai-artifacts.md`](../../fabius-praesidium/references/supply-chain-and-ai-artifacts.md) §8), and consider sealing/watermarking generated audio (`fabius-catena`). (3) Heavy local install (GPU / Apple-Silicon), young. The **TTS/STT model tier** lives in `fabius-doctrina`.

**Voice in — the polish law.** The other direction has a law of its own: **raw dictation is not the deliverable; the cleaned text is.** The browser's Web Speech API (`SpeechRecognition`, free, on-device, no key) returns an unpunctuated lowercase run-on with every false start and filler still in it — technically the speaker's words, practically not what they meant to type. One **fast-tier pass** between the transcript and the text field is the whole product: punctuate, capitalize, drop the disfluencies and the self-corrections, keep the words. The expensive parts of that pipeline (a model, routing, a key) are already present in any agent worth wiring a mic to — the polish layer is a prompt.

- **The prompt MUST forbid answering.** This is the failure the naive version ships, and it ships every time: hand a model a dictated sentence and it *replies* to it — "what time is the meeting" comes back as an answer about the meeting instead of `What time is the meeting?`. The prompt has to establish that the model is a **transcription editor**, that its entire output is the rewritten text and nothing else, and that a question in the input is **text to punctuate, never a question to answer**. Say it explicitly; the instinct it's overriding is strong.
- **Degrade to the raw transcript on any failure.** Timeout, rate-limit, malformed response, missing key, model outage — the fallback is the unpolished transcript, always. Never an error, never an empty field: **the mic must never lose words.** Polish is an upgrade layered on a path that already works on its own; the moment it becomes a dependency, someone else's 503 eats the user's sentence. (`fabius-disciplina`: the fallback is the thing to prove, since it's the path nobody exercises by hand.)
- **The boundary holds.** cohors owns the **wiring** — when the pass fires, the debounce, the fallback, the never-lose-words guarantee. `fabius-doctrina` owns the **model** that runs it, and the ASR rung underneath it (where Whisper buys punctuation and language auto-detection that Web Speech doesn't, at a metered price — doctrina states that decision rule).

The design inspiration is **Wispr Flow**, which ships exactly this pipeline (speak → clean → paste as if typed). **Stated honestly: it is closed-source and cloud-inference, with no public API to build against — there is nothing here to integrate.** The pattern is what transfers, and the pattern is the cheap half: commodity STT plus a conditioned second pass. No dependency, no license question, no row in `credits/` — an observed product shape, re-derived.

**Voice as a live agent — the split.** The polish law covers dictation: audio → text → a field, no action taken. A *live* agent is a different shape — a model holds the conversation itself, listening and speaking at once — and fabius builds it as **two machines joined by one boundary that never blocks the conversation**. Write both contracts before any transport is picked; state which side owns what.

- **The live layer owns the conversation and nothing else.** It hears and talks simultaneously and makes the turn-taking call continuously — hold the floor, yield it, or push the turn across the boundary; interruption handling, acknowledgement sounds and delivery style are its alone. Its rights stop at READ → ANALYZE → DRAFT over the conversation (drafting speech is the DRAFT rung); it is never granted a WRITE or EXECUTE tool ([`orchestration-doctrine.md`](../../fabius/references/orchestration-doctrine.md) §9). A speech-to-speech front-end exempts nothing from the ruled loop — the loop moves behind the boundary.
- **The backend owns reasoning and action.** It runs the fabius rules — routing, the §9 ladder, memory, output contracts, cooperative cancellation — and every confirmation, which crosses back through the live layer as a question and returns as an answer the backend acts on; the live layer relays a spoken "yes", it never executes one — and what a spoken answer may carry is *Confirmation classes* in [`agent-patterns.md`](agent-patterns.md): a spoken password or one-time code is the hand-off class, never relayed. Scoping and option-weighing stay in the spoken exchange, where the user's intent is richest; the backend dispatches bounded execution only after the user has fixed the scope.
- **The transcript is noisy input.** The backend repairs a mis-heard word from what the conversation has already established; codes, ids, amounts and addresses are the highest-error class and are read back before any WRITE/EXECUTE-rung step depends on them; a silence marker prompts the user to speak, it is never parsed as content; unclear intent resolves as in [`agent-patterns.md`](agent-patterns.md) *Confirmation classes* — reversible internal work acts on the likeliest reading and notes it; an outward or irreversible step gets one bounded question through the live layer, never an inferred task.
- **Barge-in is not cancel.** The user cutting speech off while delegated work is in flight is the *Durability* case in [`agent-patterns.md`](agent-patterns.md) — unknown outcome, repair never re-execute — with one new trigger: an interruption or an audio-channel restart is not evidence the action did not land. Cancellation is an explicit cooperative call the backend exposes; what happens to the in-flight step is *Durability*'s law, not a new one.
- **What crosses back.** Every backend→voice message is one of two types: a **progress note** — what changed or what is blocking, while work continues — or a **terminal note** — the result, the limit that stopped it, or the one question only the user can answer. The live layer speaks the verified takeaway, cut to what the listener needs, under the *Voice out* standard below — never raw worker output. A session ends on the user's explicit close, and an ended session issues no further backend work.
- **Whose backend.** Your own code is the backend whenever it must run the ladder, hold secrets, touch a declared record store, or take any WRITE/EXECUTE action; a provider-managed backend suits stateless answering and forfeits the result filter and the permission gate — say which controls you gave up.

**Voice out — the spoken-output standard.** The mirror of the polish law: **spoken text gets one pass at the ear — it must land on a single hearing.** parcus's *Say less* opener holds (lead with the result), then the one reason that matters; the default reply is a couple of sentences (a heuristic, not a cap — longer only on request). Strip what only a screen can hold — an identifier, a location on disk, a command line, a hash, a trace — and strip what has no spoken form: list structure, tables, code, markup, emoji; numbers, currency, units and formulas written as they are said; names the engine will mangle spelled out. Whatever has to survive exactly is not spoken: it is written to a visible surface the listener can return to, and the voice says only that it is there and what it decides; with no visible surface, redirect to the text channel rather than reading structure aloud. A spoken "done" is held to [`fabius-disciplina`](../../fabius-disciplina/SKILL.md)'s *Verify the combined result*; what audio adds is that a draft or an inference must sound like one, audibly distinct from a completed step. [`fabius-parcus`](../../fabius-parcus/SKILL.md)'s *Say less* floor — preserve exact commands, identifiers and error messages — still governs text channels; audio adds what is unsayable and what must be shown.

A full-duplex voice model with delegation shipped in a vendor API on 2026-09-10; the split above is the shape it makes practical — vendor mode names for the two backends, as of that date: a "Responses"-style delegation for the provider-run backend, "client" delegation for your own (re-check on bump).

## Evaluating the tool-caller — and, separately, the agent

- **BFCL / Gorilla** (Apache-2.0) — the standard function-calling eval (AST + executable, single/parallel/multi-turn; V4 adds agentic + web-search). Pick the model behind an agent on *evidence*, not vibes; the harness (`bfcl-eval`) reproduces it locally. **Live-updated — re-check the current table; cited scores go stale fast.**
- **τ²-Bench (tau2-bench, Sierra)** (MIT) — the realistic multi-turn *user↔agent* benchmark where **both** call tools (airline/retail/telecom/banking). The companion to BFCL that xLAM/watt-tool cards themselves cite.
- **τ³-Bench (tau3-bench, Sierra)** — the τ line pushed past tool-calling into the two places agents actually break. **τ-Knowledge** scores retrieval over a real internal corpus (a banking domain of ~698 documents across 21 product categories) and is deliberately approach-agnostic — keyword search, embedding retrieval, long context and terminal-style file exploration all compete on the same task. **τ-Voice** scores a live spoken session under accents, background noise and interruption. *Licence is per-release: τ²'s MIT does not automatically carry to the successor — check the τ³ repo.*
- **Terminal-Bench** (`harbor-framework/terminal-bench`, Apache-2.0) — the end-to-end **shell-agent** measurement frontier model cards now report beside SWE-bench: real tasks across software engineering, ML, security, data science and sysadmin (2.0 is 89 tasks; run through the **Harbor** harness).

**Match the instrument to the claim.** BFCL answers *does it emit the right call*. τ² / τ³ answer *does it hold a multi-turn job with a real user, real documents, and a real voice channel*. Terminal-Bench answers *does it finish the work in a shell*. A function-calling score is not an agent score — quoting one for the other is the most common way a model choice gets justified with the wrong number. A duplex voice shape (*the split*, above) is two instruments scored separately before any joint run — the live layer and the backend each on its own axis; doctrina's [`ml-toolkit.md`](../../fabius-doctrina/references/ml-toolkit.md) *Local speech* names the axes.

## Open tool-caller models — permissive first

| Model (HF id) | License | Note |
|---|---|---|
| **Qwen3.5** (397B-A17B · 122B-A10B · 35B-A3B sparse; 27B / 9B / 4B / 2B / 0.8B dense) | **Apache-2.0** | *Default open backbone* — Apache-2.0 across the whole family, no MAU clause and no display clause, native tool-calling, long context. Take the smallest member that clears your own eval; use Qwen-Agent / vLLM tool parsers. (Qwen3-235B-A22B is the prior line.) |
| **GLM-5.2** (zai-org) | **MIT** | The permissive heavyweight — 753B, genuinely MIT weights *and* code, agentic + tool-use + coding. (GLM-5 / GLM-5.1 prior; GLM-4.6 is two lines back.) |
| **Kimi K3** (moonshotai) | ⚠️ **"Kimi K3 License"** — HF tag `other`, **not** MIT | 2.8T total / 104B active, 1M ctx, natively multimodal and built for sustained engineering sessions and terminal orchestration — the agentic pick, after K2.5 → K2.6 → K2.7-Code. **Permissive for internal use and for embedding in your own product, but it is not MIT:** operating a **Model-as-a-Service** business (third parties controlling inputs/parameters/training data) above **$20M revenue across any consecutive 12 months** requires a separate agreement with Moonshot first, and above **100M MAU or $20M monthly revenue** you must display "Kimi K3" in the product UI. |
| **Llama 3.3 70B Instruct** | ⚠️ **Llama Community** | Ubiquitous baseline + the base for many fine-tunes — but attribution + 700M-MAU + no-train-other-models; gated. Not Apache/MIT. |
| **Hermes 4 70B** (Nous) · **Functionary v3.1** (MeetKai) · **watt-tool-70B** | ⚠️ **Llama Community** (governs) | Strong specialized tool-callers (reason-then-call / OpenAI-style parallel / DMPO multi-turn) — but all **Llama derivatives**; the repo's MIT/Apache tag does **not** override the Llama license on the weights. |
| **Salesforce xLAM-2-70b-fc-r** | ⚠️ **CC-BY-NC-4.0** | Top BFCL/τ-bench evidence of what dedicated function-call training buys — **research/eval only, do not ship.** (1B/3B/32B are Qwen-based.) |
| *dataset* **Salesforce/xlam-function-calling-60k** | CC-BY-4.0 | 60k verified function-calling samples (APIGen) — the reference *data* to train/eval a tool-caller (note: CC-BY, *not* the model's NC terms). |

**The second licence law, and this table taught it the hard way: a family's licence does not pass to its successor.** Kimi K2 was a modified-MIT with nothing but a display clause; Kimi K3 ships under its own named licence with a Model-as-a-Service revenue gate — same vendor, same product line, different terms. The model card's tag, the family's reputation, and last quarter's reading are each one release out of date. **Open the LICENSE on the exact revision you pull, every time**, and re-open it when you bump.

## Pairs with

`fabius-cohors` (the schema + the five patterns), `fabius-doctrina` (serve/fine-tune/eval the model an agent calls — doctrina owns the model, cohors owns the agent; also the TTS/STT tier behind an agent's voice), `fabius-machina` (deterministic wiring vs generative orchestration — the line; a visual Flowise flow vs a code n8n flow), `fabius-praesidium` (least-privilege tool permissions; sandbox code-as-action agents; the voice-clone consent gate), `fabius-catena` (seal/watermark an agent's generated audio for provenance), `fabius-disciplina` (a spoken "done" is a verified result) and `fabius-parcus` (the say-less floor under the spoken standard).

Informed by **system_prompts_leaks** (asgeirtj, CC0-1.0 compilation; the collected vendor prompts remain their vendors' text) — studied for the live-layer / backend split behind a spoken conversation, the typed progress-or-terminal report shape, and the spoken-output standard, re-expressed in fabius's own voice; no prompt text carried, nothing bundled. See credits/README.md.
