[
 {
  "rule": "R1",
  "title": "Routing is a measurable partition of task-space",
  "html": "<p><strong>Statement.</strong> Let each task \\(t\\) carry a load vector \\(\\ell(t)=(\\ell_M,\\ell_T,\\ell_P)\\in\\{0,1\\}^3\\), where \\(\\ell_M,\\ell_T,\\ell_P\\) indicate nonzero load on the Memory, Tools/Action, and Planning axes respectively. Equip task-space with a sigma-algebra for which \\(\\ell\\) is measurable (or use the sigma-algebra generated by its level sets). Equip the finite label space \\(L=\\{0,1\\}^3\\) with its discrete \\(\\sigma\\)-algebra \\(2^L\\). Then (i) the level sets \\(C_b=\\ell^{-1}(\\{b\\})\\) for \\(b\\in L\\) form eight labelled measurable level sets; their nonempty members partition task-space; and (ii) under a fixed priority order \\(P\\succ T\\succ M\\) on loaded axes (with the empty load reserved for parcus), the assignment \\(\\rho:L\\to\\{\\text{archivum},\\text{cohors},\\text{disciplina},\\text{parcus}\\}\\) is total and single-valued.</p>\n<p><strong>Proof.</strong> (i) The family \\(\\{C_b\\}_{b\\in L}\\) is the collection of preimages of singletons under \\(\\ell\\). Distinct singletons are disjoint, so \\(C_b\\cap C_{b'}=\\ell^{-1}(\\{b\\}\\cap\\{b'\\})=\\varnothing\\) for \\(b\\neq b'\\) (mutual exclusivity), and \\(\\bigcup_{b\\in L}C_b=\\ell^{-1}(L)=\\) the whole space (exhaustivity), since \\(\\ell\\) is defined on every task. Each \\(\\{b\\}\\in 2^L\\), so each \\(C_b\\) is measurable; hence the nonempty members of \\(\\{C_b\\}\\) form a measurable partition. As \\(|L|=2^3=8\\), there are eight possible labels and at most eight nonempty cells; exactly eight requires surjectivity of \\(\\ell\\). (ii) Define \\(\\rho(0,0,0)=\\text{parcus}\\); for \\(b\\neq 000\\), let \\(\\rho(b)\\) be the layer of the highest-priority loaded axis under \\(P\\succ T\\succ M\\): planning\\(\\to\\)disciplina, else tools\\(\\to\\)cohors, else memory\\(\\to\\)archivum. Totality: every \\(b\\in L\\) is either \\(000\\) or has a unique maximal loaded axis, so \\(\\rho(b)\\) is defined for all \\(8\\) cells. Single-valuedness: the priority order is a strict total order on the three axes, so \"highest-priority loaded axis\" is unique, giving exactly one image; \\(\\rho\\) is a function, not a relation. \\(\\square\\)</p>\n<p><strong>In fabius.</strong> The partition result establishes totality only after a classifier is supplied; it does not prove that classifier accurate. The exclusive priority map above is the initial illustrative model, superseded for composition by R13. Current routing retains the relevant owners: memory loads archivum, planning loads disciplina, and Tools/Action selects needed machinery. Tool use alone does not load cohors; that skill owns agent engineering and justified delegation.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "2026-09-08 accuracy pass: State measurability and allow empty label cells; distinguish the illustrative exclusive map from current multi-owner routing."
  ]
 },
 {
  "rule": "R2",
  "title": "The capability ladder stops at the concavity knee",
  "html": "<p><strong>Statement.</strong> Let the ladder be \\(n\\in\\{0,1,\\dots,N\\}\\). Assume value \\(V\\) is discrete-concave, i.e. its forward difference \\(\\Delta V(n):=V(n+1)-V(n)\\) is non-increasing in \\(n\\) on \\(\\{0,\\dots,N-1\\}\\); assume cost \\(C\\) is convex, i.e. \\(\\Delta C(n):=C(n+1)-C(n)\\) is non-decreasing; let utility \\(U:=V-C\\). Define \\(n^\\*=\\min\\{n:\\Delta V(n)\\le\\Delta C(n)\\}\\), with \\(n^\\*=N\\) when that set is empty. Then \\(U\\) attains its maximum at \\(n^\\*\\); the \\(\\le\\) makes a break-even rung not worth adding.</p>\n<p><strong>Proof.</strong> The marginal utility is \\(\\Delta U(n)=U(n+1)-U(n)=\\Delta V(n)-\\Delta C(n)\\). Since \\(\\Delta V\\) is non-increasing and \\(\\Delta C\\) is non-decreasing, \\(-\\Delta C\\) is non-increasing, so \\(\\Delta U\\) is a sum of two non-increasing sequences and is itself non-increasing: \\(U\\) is discrete-concave. For any \\(m\\),\n\\[U(m)-U(0)=\\sum_{k=0}^{m-1}\\Delta U(k).\\]\nBy definition of \\(n^\\*\\), \\(\\Delta U(k)=\\Delta V(k)-\\Delta C(k)>0\\) for all \\(k<n^\\*\\) (those rungs strictly increase \\(U\\)), while for \\(k\\ge n^\\*\\) monotonicity of \\(\\Delta U\\) and \\(\\Delta U(n^\\*)\\le 0\\) give \\(\\Delta U(k)\\le 0\\) (those rungs do not increase \\(U\\)). Hence partial sums of \\(\\Delta U\\) rise up to index \\(n^\\*\\) and never strictly exceed \\(U(n^\\*)\\) afterward: for \\(m<n^\\*\\), \\(U(n^\\*)-U(m)=\\sum_{k=m}^{n^\\*-1}\\Delta U(k)>0\\); for \\(m>n^\\*\\), \\(U(m)-U(n^\\*)=\\sum_{k=n^\\*}^{m-1}\\Delta U(k)\\le 0\\). Thus \\(U(n^\\*)\\ge U(m)\\) for all \\(m\\), so \\(n^\\*\\) is a global maximizer. Because the defining inequality is \\(\\le\\), a rung with \\(\\Delta V(n^\\*)=\\Delta C(n^\\*)\\) yields \\(\\Delta U(n^\\*)=0\\): climbing it cannot raise \\(U\\), so the smaller rung \\(n^\\*\\) is selected. \\(\\square\\)</p>\n<p><strong>In fabius.</strong> R2 climbs the capability ladder (inline → tool → retrieval → plan → subagent → swarm) only while the marginal value of the next rung strictly exceeds its marginal cost, halting at the smallest sufficient rung \\(n^\\*\\); the \\(\\le\\) tie-break keeps the agent inline (or one rung lower) whenever a heavier rung merely breaks even.</p>",
  "class": "real-math",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "R3",
  "title": "A call must clear its expected value of information",
  "html": "<p><strong>Statement.</strong> Fix a loss \\(L\\ge 0\\) on outcomes and let an agent face two mutually exclusive actions. <em>Inline</em>: answer now, incurring expected loss \\(a := \\mathbb{E}[L\\mid\\text{inline}]\\). <em>Call</em>: pay a fixed cost \\(c_{\\text{call}}>0\\) for an observation, then act optimally on it, incurring total expected loss \\(b + c_{\\text{call}}\\) where \\(b := \\mathbb{E}[L\\mid\\text{call}]\\) is the post-observation expected loss under the call's own optimal policy. Both expectations are taken under the agent's prior; \\(c_{\\text{call}}\\) is independent of the realized loss. <strong>Claim.</strong> The risk-minimizing action is the call iff the net value of information strictly exceeds its cost: \\(a - b > c_{\\text{call}}\\).</p>\n<p><strong>Proof.</strong> The optimum is \\(\\min\\{a,\\; b + c_{\\text{call}}\\}\\). The call is strictly preferred iff \\(b + c_{\\text{call}} < a\\), i.e. \\(a - b > c_{\\text{call}}\\); ties (\\(=\\)) and \\(a-b<c_{\\text{call}}\\) keep inline. Define \\(\\mathrm{EVOI} := a - b\\), the gross expected loss reduction the call buys. The gate routes to the call exactly when \\(\\mathrm{EVOI} > c_{\\text{call}}\\), the standard cost-of-information comparison (the expected value of information). \\(\\;\\square\\)</p>\n<p><strong>Why \\(\\mathrm{EVOI}\\) here is two-sided, unlike EVPI/EVSI \\(\\ge 0\\).</strong> Both classical EVPI and classical EVSI are nonnegative, and for the same reason: the decision-maker is assumed to <em>retain the prior-optimal (inline) action</em> and adopt the sample's recommendation only when it improves on it. Formally, with perfect information the agent picks per realization \\(\\omega\\) over an action set \\(\\mathcal{A}\\) that <em>still contains</em> inline, so \\(\\min_{\\alpha\\in\\mathcal{A}} L(\\alpha,\\omega)\\le L(\\text{inline},\\omega)\\); taking expectations, \\(\\mathbb{E}[\\min_\\alpha L]\\le a\\) and \\(\\mathrm{EVPI}=a-\\mathbb{E}[\\min_\\alpha L]\\ge 0\\). Sample information gives \\(0\\le\\mathrm{EVSI}\\le\\mathrm{EVPI}\\) by the same retain-the-default argument plus the data-processing inequality. The R3 quantity \\(a-b\\) is <em>not</em> of this protected form: \\(b\\) is the loss under the call's <em>own</em> optimal policy, whose action set need not contain the inline answer and whose noisy observation may mislead. Hence \\(a-b\\) can be <em>negative</em> — a faulty tool or wrong retrieval gives \\(b>a\\), so \\(\\mathrm{EVOI}=a-b<0<c_{\\text{call}}\\) and the gate fails <em>even at zero cost</em>. The inequality is therefore binding in both directions: a costless-but-faulty call is rejected, and a sound-but-expensive call is rejected unless its information clears \\(c_{\\text{call}}\\). \\(\\;\\square\\)</p>\n<p><strong>In fabius.</strong> R3 fires a tool/skill call only when its estimated net value \\(a-b\\) exceeds the call's cost \\(c_{\\text{call}}\\) (latency + tokens + failure risk); a faulty or redundant call with \\(b\\ge a\\) is rejected even when free, which is why this gate is two-sided rather than the trivially-nonnegative EVPI/EVSI.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Attribution overstated: the block called the gate quantity a-b 'the textbook EVSI gate'. Classical EVSI (Raiffa-Schlaifer) is itself nonnegative — by the same retain-the-default argument that makes EVPI >= 0, and with EVSI <= EVPI — precisely because the decision-maker keeps the prior-optimal (inline) action and follows the sample only when it helps. That contradicts the block's own load-bearing point that a-b can be NEGATIVE. Relabeled a-b as a net value-of-information (value of the call action vs inline), distinct from textbook EVSI.",
   "Made the EVPI/EVSI relationship explicit and correct: 0 <= EVSI <= EVPI, both nonnegative for the retain-the-default reason, whereas the R3 gate quantity is two-sided because b is computed under the call's own action set which need not contain inline. The original 'Distinction from EVPI' paragraph proved EVPI >= 0 correctly but mis-named the two-sided quantity as EVSI; the correction keeps every load-bearing step (action set still contains inline; expectation of min <= a; faulty call gives b>a so gate fails at zero cost; two-sided binding) intact.",
   "Tie-break (=) to inline preserved; the min{a, b+c} optimum and the iff are unchanged and correct."
  ]
 },
 {
  "rule": "M1",
  "title": "A second agent needs decorrelated error to pay",
  "html": "<p><strong>Statement.</strong> Let a task be completed by one agent (the author) at expected loss \\(E[L\\mid\\text{single}]\\). Adding a second agent costs \\(c_{\\text{agent}}>0\\) (tokens, latency, orchestration). Under the R3 marginal-value gate, spawn the second agent iff \\[ E[L\\mid\\text{single}] - E[L\\mid +\\text{agent}] \\;>\\; c_{\\text{agent}}. \\] Specialize to a reviewer that only catches errors. Assume a binary outcome where the artifact contains a defect; the author fails to catch it with probability \\(p_a\\in(0,1)\\) and an added checker independently misses it with probability \\(p_c\\in(0,1)\\). Loss \\(=1\\) on an uncaught defect, \\(0\\) otherwise. Then under independence the joint-miss probability is \\(p_a p_c\\), giving strict gain \\(p_a(1-p_c)\\); under error-correlation \\(\\rho\\) the gain erodes monotonically in \\(\\rho\\). The load-bearing assumption is independence — i.e. a reviewer who did not write the artifact.</p>\n<p><strong>Proof.</strong> With one agent, \\(E[L\\mid\\text{single}]=p_a\\). The defect survives only if both the author and the checker miss it. Let \\(A=\\{\\text{author misses}\\}\\), \\(C=\\{\\text{checker misses}\\}\\), with \\(\\Pr(A)=p_a\\), \\(\\Pr(C)=p_c\\). Independence gives \\[ E[L\\mid+\\text{agent}]=\\Pr(A\\cap C)=\\Pr(A)\\Pr(C)=p_a p_c. \\] Hence the marginal benefit is \\[ \\Delta = p_a - p_a p_c = p_a(1-p_c) > 0 \\quad\\text{for } p_c<1, \\] and \\(p_a p_c < p_a\\) strictly. The R3 gate fires iff \\(p_a(1-p_c)>c_{\\text{agent}}\\). Now drop independence. By Fréchet/covariance decomposition, \\[ \\Pr(A\\cap C)=\\Pr(A)\\Pr(C)+\\mathrm{Cov}(\\mathbf 1_A,\\mathbf 1_C)=p_a p_c+\\rho\\sqrt{p_a(1-p_a)p_c(1-p_c)}, \\] where \\(\\rho=\\mathrm{Corr}(\\mathbf 1_A,\\mathbf 1_C)\\). Then \\[ \\Delta(\\rho)=p_a-\\Pr(A\\cap C)=p_a(1-p_c)-\\rho\\sqrt{p_a(1-p_a)p_c(1-p_c)}, \\] so \\(\\partial\\Delta/\\partial\\rho=-\\sqrt{p_a(1-p_a)p_c(1-p_c)}\\le 0\\): the gain decreases monotonically as the two agents' errors become more correlated. At the comonotone extreme \\(\\rho\\to+\\sqrt{\\tfrac{p_a(1-p_c)}{p_c(1-p_a)}}\\) (with \\(p_c\\ge p_a\\)) we get \\(\\Pr(A\\cap C)\\to p_a\\) and \\(\\Delta\\to 0\\): a perfectly correlated checker — e.g. the same agent re-reading its own work — adds nothing while still costing \\(c_{\\text{agent}}\\). \\(\\square\\)</p>\n<p><strong>In fabius.</strong> cohors spawns a second agent only when the predicted defect-catch gain clears \\(c_{\\text{agent}}\\), and it requires the reviewer to be a fresh agent that did not author the artifact — because correlated error (the author re-checking itself) drives \\(\\Delta\\to 0\\) and fails the R3 gate.</p>",
  "class": "real-math",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "R6",
  "title": "Plan-then-bind collapses the critical path",
  "html": "<p><strong>Statement.</strong> Let an agent execute \\(n\\) independent tool calls, where call \\(i\\) incurs reasoning cost \\(r_i \\ge 0\\) (to formulate or bind the call) and tool latency \\(t_i \\ge 0\\) (to obtain the result). Assume the \\(n\\) calls are mutually independent (no call's input depends on another's output) and that tool latencies overlap freely on \\(p\\) executors. <em>Bind-as-you-go</em> interleaves reason-then-await per call, forcing a total chain \\(T_{\\mathrm{serial}}=\\sum_{i=1}^{n}(r_i+t_i)\\). <em>Plan-then-bind</em> first emits all reasoning against abstract placeholders, \\(\\sum_{i=1}^{n} r_i\\), then dispatches the now-independent calls concurrently. With \\(p \\ge n\\) executors, \\(T_{\\mathrm{plan}}=\\sum_{i=1}^{n} r_i + \\max_{i} t_i\\). For \\(n\\) identical calls \\((r_i=r,\\ t_i=t)\\), the speedup is \\(S=T_{\\mathrm{serial}}/T_{\\mathrm{plan}}=\\dfrac{n(r+t)}{nr+t}\\), and \\(S\\to n\\) as \\(r/t \\to 0\\). General lower bound (span/work): \\(T_{\\parallel}\\ge \\max\\!\\big(\\mathrm{span},\\ \\mathrm{work}/p\\big)\\).</p>\n\n<p><strong>Proof.</strong> Model the computation as a DAG: each call \\(i\\) is a chain \\(\\rho_i \\to \\tau_i\\) of weights \\(r_i\\) then \\(t_i\\); independence means no edges cross between chains. <em>Serial.</em> A single executor that binds-as-you-go imposes a total order over all \\(2n\\) nodes, so its makespan equals their summed weight \\(\\sum_i (r_i+t_i)=T_{\\mathrm{serial}}\\). <em>Plan.</em> Reasoning emitted by one agent is itself serial, contributing \\(\\sum_i r_i\\). After it completes, the \\(\\tau_i\\) form an antichain (pairwise incomparable); with \\(p\\ge n\\) each runs on its own executor starting simultaneously, so they finish at \\(\\max_i t_i\\). Hence \\(T_{\\mathrm{plan}}=\\sum_i r_i+\\max_i t_i\\). <em>Speedup.</em> For the equal case, \\(S=\\frac{n(r+t)}{nr+t}\\); fixing \\(t\\) and letting \\(r\\to 0\\), \\(S\\to \\frac{nt}{t}=n\\); equivalently \\(S=n\\cdot\\frac{r+t}{nr+t}\\uparrow n\\) monotonically as \\(r/t\\downarrow 0\\). <em>Lower bound.</em> For the imposed plan-barrier DAG, add serial reasoning edges and edges from the final reasoning node to every tool node. Its span (longest weighted path) is \\(\\sum_i r_i+\\max_i t_i\\); no schedule beats its own critical path, giving \\(T_{\\parallel}\\ge\\mathrm{span}\\). The work (total weight) is \\(\\sum_i r_i+\\sum_i t_i\\); \\(p\\) executors clear it no faster than \\(\\mathrm{work}/p\\), giving \\(T_{\\parallel}\\ge\\mathrm{work}/p\\). Taking the max yields the span/work lower bound, which \\(T_{\\mathrm{plan}}\\) attains for this barrier DAG at \\(p\\ge n\\). This is not a lower bound of the same size for a different schedule that launches each independent tool as soon as its own reasoning finishes; that schedule has a different DAG and may finish earlier. \\(\\square\\)</p>\n\n<p><strong>In fabius.</strong> R6 sketches dependencies when coordination warrants it and overlaps independent calls; tool count alone does not require a separate planning step. The idealized plan-barrier example pays \\(\\sum r_i+\\max t_i\\) instead of \\(\\sum(r_i+t_i)\\).</p>\n\n<p><strong>Honesty boundary.</strong> The scheduling law — \\(T_{\\mathrm{plan}}=\\sum r_i+\\max_i t_i\\) and \\(T_{\\parallel}\\ge\\max(\\mathrm{span},\\mathrm{work}/p)\\) — is exact and proven here over the DAG. The speedup <em>magnitude</em> \\(S\\to n\\) is the ideal for fully independent calls; fabius's multi-agent fan-out approaches it as the calls decorrelate. We claim the law, not the number: independence and free overlap may fail (shared rate limits, \\(p<n\\), latency variance), and no fabius measurement of the realized \\(S\\) exists.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "2026-09-08 accuracy pass: Scope the span to the imposed barrier DAG; the comparison does not prove a globally optimal execution schedule or mandate a plan for two calls."
  ]
 },
 {
  "rule": "R5",
  "title": "Grounding and bounded replanning are admissibility rules",
  "html": "<p><strong>Rule and trace model.</strong> Represent execution as discrete control steps containing Reason, Action, Observation, Replan, or Terminal events. A completed work cycle ends at its Observation and carries an explicit progress flag against the current verify condition. A finite trace is either an unfinished prefix or a completed run ending in Terminal; for safety/liveness classification, extend a completed run by repeating its terminal state forever. Terminal records an authorized stop or completion, not an inferred success. Let \\(\\mathrm{real}(\\tau,k)\\) contain facts established by Observations before step \\(k\\), with verified initial inputs represented by an initial Observation. The grounding guard requires \\(\\mathrm{prov}(a)\\subseteq\\mathrm{real}(\\tau,k)\\) for every Action \\(a\\) at step \\(k\\): a Reason or a pending Action cannot establish a precondition.</p>\n<p><strong>Bounded response.</strong> Choose an integer window \\(w\\ge1\\) before the loop; \\(w=3\\) is an operational default. Count consecutive completed cycles without progress, resetting the count on progress or Replan. After the count reaches \\(w\\), the next control step must be Replan or Terminal. A prefix ending exactly at the deadline has a pending obligation, not an observed violation. Any other next step violates the rule; later replanning cannot repair that bad prefix. Replan resets the counter; Terminal ends the active obligation. Together with the grounding guard, this defines \\(G\\). Both violations have finite bad prefixes under these semantics, so \\(G\\) is a <em>safety</em> property. Unbounded &ldquo;eventually replan&rdquo; is a different liveness requirement and is not claimed here. This follows the finite-prefix definitions in Alpern and Schneider, <em>Defining Liveness</em> (1985), not a new verification of an agent implementation.</p>\n<p><strong>Constraint, by policy.</strong> There is no scalar-impossibility theorem: for any nonempty feasible set \\(S=\\{\\tau:G(\\tau)\\}\\), the indicator \\(J(\\tau)=\\mathbf{1}_{G(\\tau)}\\) satisfies \\(\\arg\\max_{\\tau}J(\\tau)=S\\). It is constant on feasible traces and lower on infeasible traces; two different valid tool orders therefore cause no contradiction. Fabius chooses to enforce \\(G\\) as a mandatory admissibility constraint, independently of scores for quality, cost, or speed. A scalar representation does not establish that a learned reward or host actually enforces it.</p>\n<p><strong>In fabius.</strong> Read actual results before binding dependent actions. At the configured no-progress boundary, re-plan or report an authorized stop; never label an incomplete artifact done. This is intended control-flow policy whose enforcement must be checked in the concrete host. R5 is one of four qualitative blocks, alongside R4, R10, and M8b, in a document containing twenty-two parent rules and twenty-six blocks. The window and progress signal are operational choices, not derived constants or a convergence guarantee.</p>",
  "class": "qualitative",
  "verdict": "corrected",
  "issues": [
   "2026-09-09 focused correction: removed the false scalar-objective impossibility claim (the indicator of any nonempty feasible set is a counterexample), defined a fixed bounded-response window with explicit prefix/terminal semantics, classified it as safety, and removed the stale one-of-eighteen qualitative count. The operational rule is a chosen constraint; this correction does not re-prove all blocks."
  ]
 },
 {
  "rule": "R7",
  "title": "Branch only when sample information beats its cost",
  "html": "<p><strong>Statement.</strong> Let \\(x\\) be an unknown state with prior distribution \\(P(x)\\), let \\(a\\) range over a finite action set \\(A\\), and let \\(U(a,x)\\) be a utility with \\(\\mathbb{E}_x[|U(a,x)|]<\\infty\\) for every \\(a\\). Define the no-information value \\(V_0=\\max_{a}\\mathbb{E}_x[U(a,x)]\\). A signal \\(s\\) (with any finite branching factor, i.e. any number of possible values) has joint law \\(P(s,x)\\); define the sample-information value \\(V_1=\\mathbb{E}_s\\!\\big[\\max_a \\mathbb{E}_{x\\mid s}[U(a,x)]\\big]\\) and \\(\\mathrm{EVSI}=V_1-V_0\\). Let \\(\\mathrm{EVPI}=\\mathbb{E}_x[\\max_a U(a,x)]-\\max_a\\mathbb{E}_x[U(a,x)]\\). Then \\(0\\le\\mathrm{EVSI}\\le\\mathrm{EVPI}\\), and \\(\\mathrm{EVSI}=0\\) whenever \\(s\\perp x\\), for any branching factor.</p>\n\n<p><strong>Proof.</strong> <em>Lower bound.</em> Fix any \\(a^\\star\\in\\arg\\max_a\\mathbb{E}_x[U(a,x)]\\). For every realized \\(s\\), \\(\\max_a\\mathbb{E}_{x\\mid s}[U(a,x)]\\ge \\mathbb{E}_{x\\mid s}[U(a^\\star,x)]\\). Taking \\(\\mathbb{E}_s\\) and using the tower property \\(\\mathbb{E}_s\\mathbb{E}_{x\\mid s}[U(a^\\star,x)]=\\mathbb{E}_x[U(a^\\star,x)]=V_0\\), we get \\(V_1\\ge V_0\\), so \\(\\mathrm{EVSI}\\ge0\\). <em>Upper bound.</em> For each \\(s\\), \\(\\max_a\\mathbb{E}_{x\\mid s}[U(a,x)]\\le \\mathbb{E}_{x\\mid s}[\\max_a U(a,x)]\\), since the max of expectations is at most the expectation of the pointwise max (Jensen for the convex max functional). Taking \\(\\mathbb{E}_s\\) and applying the tower property again, \\(V_1\\le \\mathbb{E}_x[\\max_a U(a,x)]\\). Subtracting \\(V_0\\) gives \\(\\mathrm{EVSI}\\le\\mathrm{EVPI}\\). <em>Independence.</em> If \\(s\\perp x\\), then \\(P(x\\mid s)=P(x)\\), so \\(\\mathbb{E}_{x\\mid s}[U(a,x)]=\\mathbb{E}_x[U(a,x)]\\) for every \\(a\\) and every \\(s\\); hence \\(\\max_a\\mathbb{E}_{x\\mid s}[U(a,x)]=V_0\\) is constant in \\(s\\), giving \\(V_1=V_0\\) and \\(\\mathrm{EVSI}=0\\) regardless of how many values \\(s\\) takes. \\(\\square\\)</p>\n\n<p><strong>In fabius.</strong> R7 spawns a branch (a fan-out of candidates) only when an evaluator's expected information value exceeds its search cost — \\(\\mathrm{EVSI}>c_{\\text{search}}\\); since \\(\\mathrm{EVSI}=0\\) for an evaluator uninformative about the answer (no matter how wide the branching), fabius collapses to a single reason-act-observe chain when no cheap, discriminating evaluator exists.</p>",
  "class": "real-math",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "M2",
  "title": "A reducer is licensed exactly by associativity",
  "html": "<p><strong>Statement.</strong> Let \\(S\\) be a set with a binary combiner \\(\\oplus:S\\times S\\to S\\), and let inputs be finite nonempty sequences over \\(S\\). For a sequence \\(x=(x_1,\\dots,x_n)\\) define the serial left-fold \\(F(x)=(((x_1\\oplus x_2)\\oplus x_3)\\cdots\\oplus x_n)\\), and let \\(R(x)\\) be any order-preserving parallel reduction: the value at the root of an arbitrary binary tree whose left-to-right leaf order is \\(x\\), with each internal node evaluating \\(\\oplus\\) on its children. Then \\(R(x)=F(x)\\) for all inputs and all trees \\(\\iff\\) \\(\\oplus\\) is associative, i.e. \\((S,\\oplus)\\) is a semigroup. When \\(\\oplus\\) is associative, \\(R\\) is well-defined (split-point-independent) and satisfies the concatenation law \\(R(A\\Vert B)=R(A)\\oplus R(B)\\) for all nonempty \\(A,B\\); thus \\(R\\) is a semigroup homomorphism. If additionally there exists \\(e\\in S\\) with \\(e\\oplus s=s\\oplus e=s\\) for all \\(s\\), then extending the domain to admit the empty sequence with the convention \\(R(\\varnothing)=e\\) makes \\(R\\) a monoid homomorphism into \\((S,\\oplus,e)\\).</p>\n<p><strong>Proof.</strong> (\\(\\Leftarrow\\)) Suppose associativity. We show every tree equals \\(F\\) by induction on \\(n\\). For \\(n\\le 2\\) all order-preserving trees are identical, so \\(R=F\\). For \\(n\\ge 3\\), any tree splits as \\(\\oplus(R(x_{1:k}),R(x_{k+1:n}))\\) for some \\(1\\le k<n\\); by the inductive hypothesis each subtree equals its left-fold, so \\(R(x)=F(x_{1:k})\\oplus F(x_{k+1:n})\\). The generalized associative law (provable by a second induction: full reparenthesization invariance follows from the single rebracketing \\((a\\oplus b)\\oplus c=a\\oplus(b\\oplus c)\\)) gives \\(F(x_{1:k})\\oplus F(x_{k+1:n})=F(x)\\). Hence \\(R=F\\) for all trees, so \\(R\\) is split-point-independent. The concatenation law is exactly the case where the split point is the \\(A\\,/\\,B\\) boundary: \\(R(A\\Vert B)=F(A\\Vert B)=F(A)\\oplus F(B)=R(A)\\oplus R(B)\\) for nonempty \\(A,B\\) — a semigroup homomorphism requiring no identity. If a monoid identity \\(e\\) exists, setting \\(R(\\varnothing)=e\\) preserves \\(R(A\\Vert B)=R(A)\\oplus R(B)\\) when either side is empty (since \\(e\\) is two-sided), upgrading \\(R\\) to a monoid homomorphism; the empty sequence is otherwise excluded from the domain and \\(e\\) plays no role in the nonempty law.<br/>\n(\\(\\Rightarrow\\)) Contrapositive: if \\(\\oplus\\) is non-associative, pick \\(a,b,c\\) with \\((a\\oplus b)\\oplus c\\neq a\\oplus(b\\oplus c)\\). The right-leaning tree on \\((a,b,c)\\) yields \\(a\\oplus(b\\oplus c)\\) while \\(F\\) yields \\((a\\oplus b)\\oplus c\\); they differ, so \\(R\\neq F\\) on this input. Thus order-preserving-reduction\\(=\\)left-fold for all inputs forces associativity. \\(\\square\\)</p>\n<p><strong>In fabius.</strong> M2 dispatches a single reducer/synthesis agent over parallel branches only when the combiner is associative (results genuinely merge, e.g. union, sum, max, concatenation-with-dedup); when \\(\\oplus\\) is non-associative — branches are rival full answers that do not compose — it skips the merge and routes to best-of-\\(k\\), returning \\(\\arg\\max_i v(x_i)\\).</p>\n<p><strong>Boundary.</strong> The associativity theorem is proven about algebra and licenses the merge-vs-best-of-\\(k\\) decision exactly: a reducer is admissible only when the branch results form a semigroup under merge. It is a structural gate, not a numerical claim, and never overrides a value-of-information decision — when the combiner is non-associative fabius skips the merge and routes to best-of-\\(k\\).</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Domain inconsistency: inputs are defined as 'finite nonempty' sequences, yet the original proof asserts R(∅)=e to justify the homomorphism, invoking the excluded empty sequence. Corrected by (a) stating the nonempty concatenation law R(A‖B)=R(A)⊕R(B) as a SEMIGROUP homomorphism that needs no identity e, and (b) separately upgrading to a MONOID homomorphism only by explicitly extending the domain to admit ∅ with R(∅)=e.",
   "Overstated load-bearing role of e: the original 'homomorphism law' for nonempty A,B does not require e at all (it is just the split-at-boundary case of the ⇐ argument). The identity is load-bearing ONLY for the empty-sequence case. The correction makes this dependency explicit and notes e is two-sided so empty-side concatenation is preserved.",
   "Clarified that R is well-defined / split-point-independent under associativity before invoking the boundary split, so the concatenation law rests on a stated fact rather than an implicit one."
  ]
 },
 {
  "rule": "R8",
  "title": "Refine converges only under a contraction",
  "html": "<p><strong>Statement.</strong> Let \\((X,d)\\) be a metric space and \\(T:X\\to X\\) the refine operator, with iterate \\(x_{k+1}=T(x_k)\\), \\(e_k=d(x_k,x^\\*)\\). (i) <em>Hard oracle.</em> If \\(X\\) is <em>complete</em> and \\(T\\) is a contraction, i.e. there exists \\(L<1\\) with \\(d(T(x),T(y))\\le L\\,d(x,y)\\) for all \\(x,y\\), then \\(T\\) has a unique fixed point \\(x^\\*\\) and \\(e_k\\le L^k e_0\\). (ii) <em>Soft self-critique.</em> If \\(T\\) is merely non-expansive (\\(L=1\\)), no convergence is guaranteed. (iii) <em>No signal.</em> If \\(\\mathbb{E}[e_{k+1}\\mid e_k]=e_k\\) (martingale), iteration adds expected nothing.</p>\n<p><strong>Proof.</strong> (i) By the triangle inequality and contraction, for \\(m>k\\), \\(d(x_k,x_m)\\le\\sum_{i=k}^{m-1}d(x_i,x_{i+1})\\le\\sum_{i=k}^{m-1}L^i d(x_0,x_1)\\le \\frac{L^k}{1-L}d(x_0,x_1)\\to 0\\), so \\((x_k)\\) is Cauchy. <strong>Completeness</strong> supplies the limit \\(x^\\*\\in X\\); without it the Cauchy sequence may have no limit in \\(X\\) (e.g. \\(T(x)=x/2\\) on \\(\\mathbb{Q}_{>0}\\) has contraction constant \\(1/2\\), but its iterates tend to \\(0\\notin\\mathbb{Q}_{>0}\\)), and no fixed point exists — completeness cannot be dropped from the general existence guarantee. Continuity of \\(T\\) gives \\(T(x^\\*)=x^\\*\\); uniqueness follows since two fixed points \\(x^\\*,y^\\*\\) satisfy \\(d(x^\\*,y^\\*)=d(T x^\\*,T y^\\*)\\le L\\,d(x^\\*,y^\\*)\\), forcing \\(d=0\\). Finally \\(e_{k}=d(T x_{k-1},T x^\\*)\\le L\\,e_{k-1}\\), so by induction \\(e_k\\le L^k e_0\\), geometric decay.</p>\n<p>(ii) With \\(L=1\\), the Cauchy estimate collapses (the geometric series diverges). On \\(\\mathbb{R}^2\\), a quarter-turn around the fixed point \\(x^*=0\\) is an isometry with \\(e_{k+1}=e_k\\): every nonzero orbit cycles forever, never contracting. So a non-improving critic yields no contraction guarantee. (iii) If \\(\\mathbb{E}[e_{k+1}\\mid e_k]=e_k\\), then by the tower property \\(\\mathbb{E}[e_K]=e_0\\) for every \\(K\\): in expectation iteration buys zero error reduction.</p>\n<p><strong>Cap.</strong> From \\(e_k\\le L^k e_0\\le\\mathrm{tol}\\), take logs (both \\(\\ln L<0\\) and \\(\\ln(\\mathrm{tol}/e_0)<0\\)): \\(k\\ge \\dfrac{\\ln(\\mathrm{tol}/e_0)}{\\ln L}\\). For a moderate contraction \\(L\\approx 0.5\\) and a one-order-of-magnitude target \\(\\mathrm{tol}/e_0\\approx 0.1\\), \\(k\\ge \\log_2 10\\approx 3.32\\), so the smallest integer that meets tolerance is \\(\\lceil k\\rceil = 4\\) (indeed \\(0.5^3=0.125>0.1\\), while \\(0.5^4=0.0625\\le 0.1\\)). Four passes meet this particular bound. No fixed small budget follows when \\(L\\) or the target tolerance is unknown. \\(\\square\\)</p>\n<p><strong>In fabius.</strong> R8 permits a bounded refinement loop when a hard oracle supplies an attributable failure; soft critique gets a smaller operational budget, and no new signal does not justify another pass. A compiler or test suite does not make the repair operator a contraction. Contraction, non-expansiveness, and the no-signal martingale are separate model assumptions, not established properties of the corresponding agent loops. The ordinary cap of about three hard passes is a workflow setting, not a convergence guarantee; an unverified artifact must be reported as such.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Cap rounding error (load-bearing): the proof correctly derives k >= log_2 10 ~ 3.32 but then writes ceil(k) ~ 3. The ceiling of 3.32 is 4, and at k=3 one has e_3 = 0.5^3 e_0 = 0.125 e_0 > 0.1 e_0 = tol, so 3 iterations do NOT reach tolerance. Fixed to ceil(k) = 4, with the check 0.5^3=0.125>0.1 and 0.5^4=0.0625<=0.1 shown explicitly, and the budget restated as ~3-4.",
   "Propagation to 'In fabius': the hard budget was stated as '~3' resting on the wrong rounding; corrected to '~3-4' so the routing claim matches the corrected arithmetic.",
   "Minor rigor: added 'by the tower property' to justify E[e_K]=e_0 in the martingale clause (clause was correct, justification was implicit).",
   "2026-09-08 accuracy pass: Repair the incomplete-space contraction example and fixed-point reference; separate hard-oracle availability from contraction and illustrative pass counts."
  ]
 },
 {
  "rule": "M3",
  "title": "Optimal verification depth exists only because q&gt;0",
  "html": "<p><strong>Statement.</strong> Let verification effort \\(v\\ge 0\\) on a route with measured per-route failure probability \\(q\\in[0,1]\\) and failure cost \\(C_{\\mathrm{fail}}>0\\). Let the detection rate \\(r:[0,\\infty)\\to[0,1)\\) be twice differentiable, strictly increasing and strictly concave (\\(r'>0,\\ r''<0\\)), with \\(r(0)=0\\); note that because \\(r\\) is concave, increasing and bounded above by \\(1\\), necessarily \\(r'(v)\\downarrow 0\\) as \\(v\\to\\infty\\). The total cost is \\[ J(v)=q\\,(1-r(v))\\,C_{\\mathrm{fail}}+\\kappa v,\\qquad \\kappa>0. \\] Then \\(J\\) is strictly convex for \\(q>0\\) and strictly increasing linear for \\(q=0\\); in either case it has a unique minimizer \\(v^\\*\\), which is interior and positive iff \\(q\\,C_{\\mathrm{fail}}\\,r'(0)>\\kappa\\), in which case \\(v^\\*=(r')^{-1}\\!\\big(\\kappa/(qC_{\\mathrm{fail}})\\big)\\), and otherwise \\(v^\\*=0\\). Moreover \\(\\partial v^\\*/\\partial q>0\\) on the interior regime, and \\(q=0\\Rightarrow v^\\*=0\\).</p>\n<p><strong>Proof.</strong> Differentiate: \\(J'(v)=-q\\,C_{\\mathrm{fail}}\\,r'(v)+\\kappa\\) and \\(J''(v)=-q\\,C_{\\mathrm{fail}}\\,r''(v)\\). For \\(q>0\\), since \\(C_{\\mathrm{fail}}>0\\) and \\(r''<0\\) we get \\(J''>0\\) everywhere, so \\(J\\) is strictly convex on \\([0,\\infty)\\); hence any stationary point of the constrained problem is the unique global minimizer (KKT for a convex objective on \\([0,\\infty)\\)). The first-order condition with the constraint \\(v\\ge 0\\): if \\(J'(0)=\\kappa-q\\,C_{\\mathrm{fail}}\\,r'(0)\\ge 0\\), then since \\(r'\\) is strictly decreasing, \\(-qC_{\\mathrm{fail}}r'\\) is strictly increasing, so \\(J'\\ge0\\) on \\([0,\\infty)\\), giving the boundary solution \\(v^\\*=0\\). If instead \\(q\\,C_{\\mathrm{fail}}\\,r'(0)>\\kappa\\), then \\(J'(0)<0\\). Now the boundedness of \\(r\\) is load-bearing: a concave increasing \\(r\\) with \\(\\lim_{v\\to\\infty}r'(v)=L>0\\) would obey \\(r(v)\\ge r(0)+Lv\\to\\infty\\), contradicting \\(r<1\\); hence \\(L=0\\) and \\(J'(v)\\to\\kappa>0\\). By strict monotonicity and continuity of \\(J'\\) there is therefore a unique interior root \\[ r'(v^\\*)=\\frac{\\kappa}{q\\,C_{\\mathrm{fail}}}\\ \\Longrightarrow\\ v^\\*=(r')^{-1}\\!\\Big(\\tfrac{\\kappa}{qC_{\\mathrm{fail}}}\\Big)>0, \\] using that \\(r'\\) is a strictly decreasing bijection from \\([0,\\infty)\\) onto \\((0,r'(0)]\\) and \\(\\kappa/(qC_{\\mathrm{fail}})<r'(0)\\) lies in that range. Comparative statics: implicitly differentiate \\(r'(v^\\*)=\\kappa/(qC_{\\mathrm{fail}})\\): \\(r''(v^\\*)\\,\\partial_q v^\\*=-\\kappa/(q^2C_{\\mathrm{fail}})\\), so \\[ \\frac{\\partial v^\\*}{\\partial q}=\\frac{-\\kappa}{q^2 C_{\\mathrm{fail}}\\,r''(v^\\*)}>0 \\] because \\(r''<0\\): more verification where failure is more likely. Finally, set \\(q=0\\): then \\(J(v)=\\kappa v\\) is strictly increasing, minimized at \\(v^\\*=0\\); equivalently the threshold \\(q\\,C_{\\mathrm{fail}}\\,r'(0)>\\kappa\\) fails for \\(q=0\\) since the left side is \\(0<\\kappa\\). The optimal depth is positive only because \\(q>0\\). \\(\\square\\)</p>\n<p><strong>q estimation.</strong> \\(q\\) is not known a priori; maintain a Beta–Bernoulli posterior per route. With prior \\(q\\sim\\mathrm{Beta}(\\alpha_0,\\beta_0)\\) and \\(s\\) observed failures in \\(n\\) i.i.d. executions, conjugacy gives the posterior \\(\\mathrm{Beta}(\\alpha_0+s,\\ \\beta_0+n-s)\\), and the plug-in posterior-mean estimate \\(\\hat q=(\\alpha_0+s)/(\\alpha_0+\\beta_0+n)\\) feeds the threshold and \\(v^\\*\\) online; as \\(n\\) grows the posterior concentrates and \\(v^\\*\\) tracks the true rate. (Plugging \\(\\hat q\\) into the nonlinear threshold is a point approximation, not the full posterior-averaged Bayes decision.)</p>\n<p><strong>In fabius.</strong> M3 sets the corrector/verification depth on each route: spend nonzero verification effort exactly when \\(\\hat q\\,C_{\\mathrm{fail}}\\,r'(0)>\\kappa\\) (deeper where the Beta–Bernoulli \\(\\hat q\\) is higher), and attach no corrector to routes that never fail (\\(\\hat q\\approx 0\\Rightarrow v^\\*=0\\)) — YAGNI as a theorem.</p><p><strong>Operational boundary.</strong> This model prices optional verification effort; it does not authorize removing mandatory security, validation, accessibility, or requested checks. Those requirements form a separate feasibility floor. A route-specific detection curve and calibrated failure posterior are inputs to the model, not measurements supplied automatically by installing the plugin.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Limit step under-justified: proof claims J'(v)→κ from 'r'(v)→L≥0' but that only gives J'(v)→κ−qC_fail·L; the needed L=0 is not implied by r''<0 alone. Fixed by invoking boundedness of r (concave + increasing + r<1 ⟹ r'→0), now stated as a load-bearing assumption/step.",
   "Malformed expression 'r'(v)→L≥0≤r'(0)' removed and replaced with a correct argument.",
   "Made explicit that r' is a bijection onto (0, r'(0)] and that the threshold value κ/(qC_fail) lies in that range, so the interior root exists and is finite (ruling out v*=+∞).",
   "Added 'i.i.d.' to the Beta–Bernoulli observations and noted the plug-in posterior-mean is a point approximation, not the full posterior-averaged Bayes decision.",
   "2026-09-08 accuracy pass: Correct the q=0 convexity boundary and distinguish optional effort from mandatory verification floors and measured calibration."
  ]
 },
 {
  "rule": "M4",
  "title": "Reflect-then-retry has a myopic stopping threshold",
  "html": "<p><strong>Statement.</strong> After a failed attempt, index candidate retries by \\(k=1,2,\\ldots\\). Let \\(V_{\\text{fix}}>0\\) be the value of success, \\(c_{\\text{retry}}>0\\) the cost of one more reflect-and-retry round, and \\(p_k\\in[0,1]\\) its conditional success probability given the available history. Assume risk-neutral additive utility and a <em>myopic one-step lookahead</em>: compare stopping now with making exactly one more attempt and then stopping. The marginal value is \\(\\Delta V_k=p_kV_{\\text{fix}}-c_{\\text{retry}}\\). Continue iff \\(p_kV_{\\text{fix}}>c_{\\text{retry}}\\); stop otherwise, including equality.</p>\n<p><strong>Threshold argument.</strong> Stopping has value \\(0\\); the next attempt has expected value \\(p_kV_{\\text{fix}}+(1-p_k)\\cdot0-c_{\\text{retry}}\\). The strict positive-value comparison gives the stated rule. If a reflection has zero conditional information about the failure cause, the cause posterior is unchanged. Its success estimate is unchanged only when the conditional action policy and relevant problem state also stay fixed. Even then, redundancy alone does not imply stopping: \\(p_k=0.75\\) and \\(c_{\\text{retry}}/V_{\\text{fix}}=0.1\\) leave positive normalized value \\(0.65\\). An unchanged estimate remains below the bar only if it was already below or at it. Repeated-cause stopping is a separate operational heuristic, not a consequence of zero information.</p>\n<p><strong>Geometric illustration and boundary.</strong> Suppose, additionally, \\(p_k=p_1\\rho^{k-1}\\), with \\(0<\\rho<1\\); this decay is an assumption, not something reflection guarantees. Write \\(g_k=p_kV_{\\text{fix}}=g_1\\rho^{k-1}\\), where \\(g_1\\ge0\\). The first rejected candidate retry is\n\\[\nk^\\star=\\min\\{k\\ge1:g_k\\le c_{\\text{retry}}\\}\n=\\begin{cases}\n1, & g_1\\le c_{\\text{retry}},\\\\\n1+\\left\\lceil\\dfrac{\\log(g_1/c_{\\text{retry}})}{\\log(1/\\rho)}\\right\\rceil, & g_1>c_{\\text{retry}}.\n\\end{cases}\n\\]\nFor the second branch, taking logs of \\(g_1\\rho^{k-1}\\le c_{\\text{retry}}\\) gives \\(k-1\\ge\\log(g_1/c_{\\text{retry}})/\\log(1/\\rho)\\); the ceiling chooses the first integer. The first branch covers equality, sub-cost gain, and zero without an undefined logarithm or a nonpositive retry index. Along a path of failures with no earlier stop, \\(k^\\star-1\\) retries are executed; \\(k^\\star\\) is the decision rejecting the next retry. At \\(\\rho=1/2\\), gain/cost ratios \\(4,1,1/4,0\\) give rejection indices \\(3,1,1,1\\), respectively, and executed-retry counts \\(2,0,0,0\\). A success or external stop can end the run earlier.</p>\n<p><strong>In fabius.</strong> Continue while a justified estimate of the next retry's value clears its cost. Escalation when a reflection repeats a cause without a new hypothesis, and a budget of about three retries, are workflow heuristics. They are not general consequences of the geometric example. The mathematical threshold is optimal only for the stated one-step comparison; it makes no claim of globally optimal adaptive retrying or calibrated success estimates.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Overstated inequality: under zero conditional mutual information I(R_k;cause|R_<k)=0 the posterior over the cause is UNCHANGED, which gives p_{k+1}=p_k (no lift), not p_{k+1}<=p_k. The strict/weak decrease '<=' requires an extra assumption not stated. Corrected to equality p_{k+1}=p_k, which is precisely what 'do not lift the hazard' asserts, and rephrased the STOP step to follow from 'cannot clear a threshold it already failed' rather than from a manufactured decrease.",
   "Replaced 'approx 0' with exact '=0' in the conditional-MI premise so the equality conclusion is clean.",
   "Softened 'empirically typical Reflexion regimes' to 'Reflexion-scale regimes' to avoid implying a measured fabius/Reflexion result; the k*≈3 number is an illustration of the closed form, not a claimed empirical fabius datum. Closed-form k* and numeric (g1/c=4, rho=1/2 -> 3) verified correct.",
   "2026-09-09 focused correction superseding the earlier blanket closed-form verification: use the weak crossing at equality, define the first rejected retry from k=1, branch at g1<=cost to cover zero/sub-cost gain, and distinguish rejection index from completed retries. Ratios 4,1,1/4,0 at rho=1/2 yield indices 3,1,1,1. Also state the unchanged-policy/state premise for unchanged success and separate repeated-cause stopping from the one-step value theorem."
  ]
 },
 {
  "rule": "M5",
  "title": "Accept a rewrite only on a held-out risk drop",
  "html": "<p><strong>Statement.</strong> Let \\(\\mathcal{P}\\) be a finite class of candidate prompts. Each prompt \\(p\\) has a true risk \\(R(p)=\\mathbb{E}_{x\\sim\\mathcal{D}}[\\ell(p,x)]\\) with bounded loss \\(\\ell(p,x)\\in[0,1]\\). Let \\(D_{\\mathrm{val}}=\\{x_1,\\dots,x_n\\}\\) be drawn i.i.d. from \\(\\mathcal{D}\\), independent of how \\(\\mathcal{P}\\) was assembled, and let \\(\\widehat{R}(p)=\\frac1n\\sum_{i=1}^n \\ell(p,x_i)\\). Let \\(\\hat p\\in\\arg\\min_{p\\in\\mathcal{P}}\\widehat{R}(p)\\) (ERM). Then with probability at least \\(1-\\delta\\),\n\\[ R(\\hat p)\\;\\le\\;\\min_{p\\in\\mathcal{P}}R(p)\\;+\\;2\\sqrt{\\frac{\\ln|\\mathcal{P}|+\\ln(2/\\delta)}{2n}}. \\]</p>\n\n<p><strong>Proof.</strong> Fix \\(p\\). The terms \\(\\ell(p,x_i)\\) are i.i.d. in \\([0,1]\\) with mean \\(R(p)\\). A concentration inequality gives, for any \\(t>0\\), \\(\\Pr[|\\widehat{R}(p)-R(p)|>t]\\le 2e^{-2nt^2}\\). Set \\(t=\\varepsilon:=\\sqrt{(\\ln|\\mathcal{P}|+\\ln(2/\\delta))/(2n)}\\), so \\(2e^{-2n\\varepsilon^2}=\\delta/|\\mathcal{P}|\\). Union-bounding over the \\(|\\mathcal{P}|\\) prompts,\n\\[ \\Pr\\!\\Big[\\exists\\, p\\in\\mathcal{P}:|\\widehat{R}(p)-R(p)|>\\varepsilon\\Big]\\le|\\mathcal{P}|\\cdot\\frac{\\delta}{|\\mathcal{P}|}=\\delta. \\]\nSo on an event \\(E\\) of probability \\(\\ge 1-\\delta\\), \\(|\\widehat{R}(p)-R(p)|\\le\\varepsilon\\) holds simultaneously for every \\(p\\). Let \\(p^\\star\\in\\arg\\min_{p}R(p)\\). On \\(E\\):\n\\[ R(\\hat p)\\;\\le\\;\\widehat{R}(\\hat p)+\\varepsilon\\;\\le\\;\\widehat{R}(p^\\star)+\\varepsilon\\;\\le\\;R(p^\\star)+2\\varepsilon, \\]\nwhere the first and third steps use uniform concentration and the middle step uses that \\(\\hat p\\) minimizes \\(\\widehat{R}\\) (hence \\(\\widehat{R}(\\hat p)\\le\\widehat{R}(p^\\star)\\)). Substituting \\(\\varepsilon\\) yields the claim. \\(\\square\\)</p>\n\n<p><strong>In fabius.</strong> M5 accepts a rewrite \\(p'\\) over \\(p\\) only when \\(\\widehat{R}(p')<\\widehat{R}(p)\\) on the held-out \\(D_{\\mathrm{val}}\\); the bound certifies that an empirical drop transfers to true risk up to \\(2\\varepsilon\\), so a rewrite that merely \"sounds better\" but shows no measured \\(\\widehat{R}\\) delta is rejected, and the gate tightens as \\(n\\) grows.</p>",
  "class": "real-math",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "M6",
  "title": "A verified skill is a memoized sub-solution",
  "html": "<p><strong>Statement.</strong> Let a task decompose into subproblems whose dependency relation forms a DAG with \\(n\\) distinct nodes, and assume (i) <em>optimal substructure</em> — an optimal solution is composed of optimal solutions to its subproblems — and (ii) <em>overlapping subproblems</em> — the naive recursion revisits the same nodes exponentially often. Model a verified, retrieval-keyed skill as a memo table \\(M[s]\\): on a hit, return the stored solution in \\(O(1)\\) plus a verify-predicate \\(V(s)\\); on a miss, compute and store. Then memoized evaluation performs \\(\\Theta(2^n)\\) → \\(O(n)\\) work (each subproblem solved once), and the expected per-call cost is \\((1-h)\\,c_{\\text{compute}} + h\\,c_{\\text{lookup}}\\) with hit rate \\(h\\) and \\(c_{\\text{lookup}} \\ll c_{\\text{compute}}\\).</p>\n<p><strong>Proof.</strong> Naive recomputation of a recurrence whose call tree branches (e.g. \\(T(n)=2T(n-1)+\\Theta(1)\\)) satisfies \\(T(n)=\\Theta(2^n)\\): the tree has exponentially many leaves because each overlapping subproblem is re-expanded independently. Now equip the recursion with \\(M\\). Charge each invocation to one of two buckets. A node \\(s\\) is <em>first-seen</em> at most once: at that point \\(M[s]\\) is empty, we pay \\(c_{\\text{compute}}(s)\\) and write the result; by optimal substructure this stored value is a correct optimal sub-solution, so it never needs recomputation. Every later invocation on \\(s\\) hits \\(M\\) and pays only \\(c_{\\text{lookup}}+V(s)=O(1)\\). First-seen work therefore sums over the \\(n\\) distinct nodes once: \\(\\sum_{s} c_{\\text{compute}}(s) = O(n)\\) for \\(O(1)\\) per-node cost (more generally \\(O(n)\\) calls each doing bounded local work over its in-edges, \\(O(n+E)\\)). Hit work is absorbed into the \\(O(1)\\) charge of the calling edge. Summing the disjoint buckets gives \\(O(n)\\), collapsing the exponential. Amortized over a call stream with hit rate \\(h\\), expected cost \\(=(1-h)\\,c_{\\text{compute}}+h\\,c_{\\text{lookup}}\\); since \\(c_{\\text{lookup}}\\ll c_{\\text{compute}}\\), this is monotone decreasing in \\(h\\) and tends to \\(c_{\\text{lookup}}\\). \\(\\square\\)</p>\n<p><strong>In fabius.</strong> M6 forces <em>retrieve-before-plan</em>: before spending a compute step, query the skill/memory store keyed by the subproblem; a verified hit is returned at lookup cost and a miss is computed once and written back — so identical sub-solutions are reused, never re-derived, and \"supersede-don't-duplicate\" keeps each key solved a single time.</p>",
  "class": "real-math",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "M7",
  "title": "Eviction is lossless because addresses are a code",
  "html": "<p><strong>Statement.</strong> Fix a fast tier of capacity \\(B\\) (bytes of window occupancy) and an unbounded slow tier. An eviction <code>write=EVICT</code> moves a fact \\(f\\) of length \\(\\ell(f)\\) to an addressed page and leaves in the window a pointer <code>[[slug]]</code> of length \\(\\ell(\\text{slug})\\). Assume (i) addresses are drawn from a fixed alphabet of size \\(D\\); (ii) the addressing scheme is uniquely decodable — distinct stored facts always receive distinct, unambiguously parseable slugs, so the address-to-page map is injective and total; and (iii) eviction is byte-gated: M7 evicts \\(f\\) only when \\(\\ell(\\text{slug})\\le\\ell(f)\\) (the pointer is no longer than the fact it replaces). Let the multiset of address lengths in use be \\(\\{\\ell_1,\\dots,\\ell_n\\}\\). Then a slug code with these lengths can coexist (the prefix-code sum is satisfiable), eviction is byte-exact reversible, and each eviction leaves total stored information non-decreasing while never increasing window occupancy (it drops it by \\(\\ell(f)-\\ell(\\text{slug})\\ge 0\\)).</p>\n\n<p><strong>Proof.</strong> <em>Necessity.</em> For any uniquely-decodable code, consider the \\(k\\)-fold extension. The generating identity \\(\\left(\\sum_{i=1}^{n} D^{-\\ell_i}\\right)^{k}=\\sum_{m=k\\ell_{\\min}}^{k\\ell_{\\max}} A_m\\,D^{-m}\\), where \\(A_m\\) counts length-\\(k\\) source strings whose concatenated codewords have total length \\(m\\). Unique decodability forces injectivity onto \\(D\\)-ary strings of length \\(m\\), so \\(A_m\\le D^{m}\\), giving each term \\(A_m D^{-m}\\le 1\\); the sum has at most \\(k\\ell_{\\max}-k\\ell_{\\min}+1\\le k\\,\\ell_{\\max}\\) terms (as \\(\\ell_{\\min}\\ge 1\\)), so \\(\\left(\\sum_i D^{-\\ell_i}\\right)^{k}\\le k\\,\\ell_{\\max}\\). Taking \\(k\\)-th roots and \\(k\\to\\infty\\), the right side \\((k\\ell_{\\max})^{1/k}\\to 1\\), yielding \\(\\sum_i D^{-\\ell_i}\\le 1\\). <em>Sufficiency.</em> Conversely, if \\(\\sum_i D^{-\\ell_i}\\le 1\\), sort lengths and assign codewords greedily on the \\(D\\)-ary tree; the inequality guarantees enough unused subtrees, producing a prefix (hence uniquely-decodable) code. Thus such a code exists iff \\(\\sum_i D^{-\\ell_i}\\le 1\\). By assumption (ii) the slug scheme IS uniquely decodable, so its lengths satisfy the inequality, and decoding recovers each slug exactly; following the address fetches \\(f\\) byte-for-byte. Hence eviction is a bijection between \\((f \\text{ in window})\\) and \\((\\text{slug in window}, f \\text{ on page})\\): no information is destroyed, only relocated, so stored information is non-decreasing. By assumption (iii), occupancy changes by \\(-(\\ell(f)-\\ell(\\text{slug}))\\le 0\\), so each eviction is byte-exact reversible and non-increasing in window occupancy. \\(\\square\\)</p>\n\n<p><strong>In fabius.</strong> M7 routes a fact to <code>write=EVICT</code> when the window would exceed \\(B\\), but only when \\(\\ell(\\text{slug})\\le\\ell(f)\\): flush \\(f\\) to an addressed page, keep only <code>[[slug]]</code>, and trust that the unique decodability of the address guarantees byte-exact recall — so the eviction frees \\(\\ell(f)-\\ell(\\text{slug})\\ge 0\\) of budget with zero loss, monotonically drawing occupancy back toward \\(\\le B\\).</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Occupancy overstatement: the original wrote the savings \\(\\ell(f)-\\ell(\\text{slug})\\ge 0\\) as if self-evident, but nothing in assumptions (i)-(ii) forces a slug to be no longer than the fact it replaces. A short fact with a long slug would INCREASE occupancy, breaking both the '\\(\\ge 0\\)' claim and 'occupancy stays \\(\\le B\\)'. This non-negativity is a load-bearing hypothesis, not a consequence of unique decodability. Fixed by adding explicit byte-gating assumption (iii): M7 evicts only when \\(\\ell(\\text{slug})\\le\\ell(f)\\).",
   "'window occupancy stays \\(\\le B\\)' is too strong as a per-eviction guarantee: eviction is triggered precisely because occupancy would exceed \\(B\\), and a single eviction need not restore \\(\\le B\\). The defensible claim is that each (gated) eviction is non-increasing in occupancy, monotonically driving it back toward \\(\\le B\\). Restated accordingly in both the Statement and the 'In fabius' note.",
   "Minor exposition tightening in the McMillan step: made explicit that each term \\(A_m D^{-m}\\le 1\\) and that the number of terms is \\(\\le k\\ell_{\\max}\\) (using \\(\\ell_{\\min}\\ge 1\\)), and that \\((k\\ell_{\\max})^{1/k}\\to 1\\). The original's 'the right side's exponent vanishes' was correct but terse; no mathematical error there."
  ]
 },
 {
  "rule": "R9",
  "title": "Retrieval under a budget is submodular knapsack",
  "html": "<p><strong>Statement.</strong> Let \\(P\\) be a set of candidate pages, each with token length \\(\\mathrm{len}(p)>0\\), and a budget \\(B\\). Let \\(f:2^{P}\\to\\mathbb{R}_{\\ge 0}\\) be the relevance of a retrieved set, assumed <em>monotone</em> (\\(A\\subseteq B\\Rightarrow f(A)\\le f(B)\\)) and <em>submodular</em>: for all \\(A\\subseteq B\\) and \\(p\\notin B\\), \\(f(A\\cup\\{p\\})-f(A)\\ge f(B\\cup\\{p\\})-f(B)\\). Submodularity is the realistic model: two pages covering the same fact have overlapping relevance, so each added page's marginal usefulness diminishes. We maximize \\(f(S)\\) subject to \\(\\sum_{p\\in S}\\mathrm{len}(p)\\le B\\). (i) Plain marginal-gain greedy (pick the feasible page of largest <em>absolute</em> gain, ignoring cost) has unbounded approximation ratio. (ii) Cost-benefit greedy with partial enumeration of seed sets of size \\(\\le 3\\) achieves \\(1-1/e\\) (a standard submodular-knapsack result — plain modified greedy alone gives only \\(1-1/\\sqrt{e}\\), enumeration recovers \\(1-1/e\\)).</p>\n<p><strong>Proof.</strong> <em>(i)</em> Fix an integer \\(B\\ge 2\\) and a parameter \\(M>2\\). Construct one expensive page \\(p_0\\) with \\(\\mathrm{len}(p_0)=B\\) and singleton value \\(M\\), and \\(B\\) cheap pages \\(q_1,\\dots,q_B\\), each \\(\\mathrm{len}(q_i)=1\\), with \\(f\\) additive over \\(\\{q_i\\}\\) and per-page gain \\(M/B + 1\\) (additive functions are submodular). Plain marginal-gain greedy compares absolute gains while feasible: the first move sees \\(p_0\\) with gain \\(M\\) versus each \\(q_i\\) with gain \\(M/B+1<M\\), so it takes \\(p_0\\); now \\(\\mathrm{len}\\) is exhausted and greedy returns \\(f=M\\). OPT instead packs all \\(B\\) cheap pages, \\(f(\\mathrm{OPT})=B(M/B+1)=M+B\\). The ratio \\(M/(M+B)\\to1\\) here, so reverse the values: set \\(p_0\\) cheap and absolute-gain-largest but density-poor — \\(\\mathrm{len}(p_0)=1,\\ f(\\{p_0\\})=2\\) — and let \\(q_1,\\dots,q_B\\) each have \\(\\mathrm{len}=1\\) and gain \\(2-\\varepsilon\\), but add a single page \\(r\\) with \\(\\mathrm{len}(r)=B,\\ f\\)-gain \\(M\\gg B\\) that greedy never reaches because every unit step has gain \\(2\\ge\\) the *first* comparison only if \\(M\\) is hidden behind a feasibility wall. Cleanly: take \\(\\mathrm{len}(p_0)=1,\\,f(\\{p_0\\})=2\\) and one page \\(r\\) with \\(\\mathrm{len}(r)=B,\\,f(\\{r\\})=M\\); with \\(f\\) additive and \\(M>2\\), greedy takes \\(r\\) and matches OPT, so cost-ignoring greedy's failure needs budget blocking, not value blocking. The canonical unbounded instance: pages \\(a\\) (\\(\\mathrm{len}=1,\\ \\text{gain}=1+\\varepsilon\\)) and \\(b\\) (\\(\\mathrm{len}=B,\\ \\text{gain}=M\\)), additive, with the modification that after greedy commits to the locally-largest *unit-feasible* gain it is forced to spend on \\(a\\)-type fillers. Concretely give greedy \\(B\\) filler pages each \\(\\mathrm{len}=1,\\ \\text{gain}=1+\\varepsilon\\), so it consumes the whole budget on fillers for total \\((1+\\varepsilon)B\\), while OPT takes the single page \\(b\\) of cost \\(B\\) and value \\(M\\). Choosing \\(M\\gg(1+\\varepsilon)B\\) yields ratio \\((1+\\varepsilon)B/M\\to0\\). Plain greedy is arbitrarily bad. <em>(ii)</em> For each seed \\(S_0\\) with \\(|S_0|\\le 3\\), run cost-benefit greedy (add the feasible \\(p\\) maximizing the density \\(\\bigl(f(S\\cup\\{p\\})-f(S)\\bigr)/\\mathrm{len}(p)\\)) on \\(P\\setminus S_0\\); return the best result over all seeds. Monotone submodularity gives, at every greedy state \\(S\\), the covering inequality \\(f(\\mathrm{OPT})-f(S)\\le\\sum_{p\\in\\mathrm{OPT}\\setminus S}\\bigl(f(S\\cup\\{p\\})-f(S)\\bigr)\\); dividing each term by \\(\\mathrm{len}(p)\\) and re-weighting by \\(\\mathrm{len}(p)\\) shows the densest feasible pick captures at least a \\(\\mathrm{len}(p)/B\\) fraction of the residual gap, which drives the standard \\(1-1/e\\) recursion. Enumerating the (at most) three highest-value elements of \\(\\mathrm{OPT}\\) as a seed removes the boundary item that a density-only argument can drop on the last partial step. Hence the returned \\(S\\) satisfies \\(f(S)\\ge(1-1/e)\\,f(\\mathrm{OPT})\\). \\(\\square\\)</p>\n<p><strong>In fabius.</strong> R9 governs how archivum fills a retrieval token budget: never rank pages by raw relevance, rank by relevance-per-token (cost-benefit) with a small seed sweep, so one long page can't crowd out several denser ones — exactly the knapsack failure plain top-\\(k\\) commits.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Part (i) as written is non-rigorous and internally contradictory: it openly thrashes ('No — it takes...', 'instead let...', 'with the twist that...') and conflates two distinct failure mechanisms (largest-absolute-gain vs budget-exhaustion). The numeric setup f({p1})=2, f({p2})=M with M>2 actually makes cost-ignoring greedy pick the GOOD item p2 and match OPT, so it does not establish unboundedness — the stated example proves the opposite of the claim. The claim itself (plain greedy unbounded) is true, but the proof must reach it via a valid instance.",
   "The expression in (ii), \\(\\sum_{p}\\frac{f(S\\cup\\{p\\})-f(S)}{\\mathrm{len}(p)}\\mathrm{len}(p)\\), trivially cancels to \\(\\sum_p (f(S\\cup\\{p\\})-f(S))\\); writing the density and its inverse weight in the same term is a dimensional slip. Corrected to state the submodular covering inequality first, then divide/re-weight to justify the density rule.",
   "Attribution was thin: the bound credited 'Khuller–Moss–Naor; Sviridenko' without noting that KMN's plain modified greedy gives only \\(1-1/\\sqrt{e}\\) and that partial enumeration (the load-bearing ingredient the Statement names) is exactly what lifts it to \\(1-1/e\\). Made explicit so the (1-1/e) bound is not silently attributed to greedy alone."
  ]
 },
 {
  "rule": "R9b",
  "title": "Index-first prefiltering is a mutual-information cut",
  "html": "<p><strong>Statement.</strong> Let the target \\(X\\) be drawn over a finite universe \\(U\\), and let a cheap symbolic index emit a value \\(Y\\) that deterministically restricts the candidate set to \\(C(Y)=\\{x: \\text{test passes}\\}\\subseteq U\\). Assuming \\(X\\) is uniform on \\(U\\) and uniform on \\(C(Y)\\) given \\(Y\\), the search entropy falls from \\(H(X)=\\log_2|U|\\) to \\(H(X\\mid Y)=\\mathbb{E}_Y[\\log_2|C(Y)|]\\), and the expected reduction is the mutual information \\(I(X;Y)=H(X)-H(X\\mid Y)\\ge 0\\). Under per-comparison costs \\(c_{\\text{index}},c_{\\text{rerank}}\\), the two-stage cascade \\(c_{\\text{index}}|U|+c_{\\text{rerank}}|C|\\) beats single-stage \\(c_{\\text{rerank}}|U|\\) iff \\(c_{\\text{index}}/c_{\\text{rerank}}<1-|C|/|U|\\); and if the test is relevance-monotone (it never excludes a truly relevant item), recall@\\(C\\approx 1\\).</p>\n\n<p><strong>Proof.</strong> Identifying entropy with the bits needed to locate \\(X\\): uniformity gives \\(H(X)=\\log_2|U|\\). Conditioning on \\(Y\\), \\(X\\) is uniform on \\(C(Y)\\), so \\(H(X\\mid Y=y)=\\log_2|C(y)|\\) and \\(H(X\\mid Y)=\\mathbb{E}_Y[\\log_2|C(Y)|]\\). By definition \\(I(X;Y)=H(X)-H(X\\mid Y)\\), and by Jensen (concavity of \\(\\log\\)) or the non-negativity of KL divergence, \\(I(X;Y)\\ge 0\\), with equality iff \\(X\\perp Y\\) — i.e. a useless index. Thus the index removes exactly \\(I(X;Y)\\) bits of search.</p>\n<p>For cost: \\(c_{\\text{index}}|U|+c_{\\text{rerank}}|C|<c_{\\text{rerank}}|U| \\iff c_{\\text{index}}|U|<c_{\\text{rerank}}(|U|-|C|)\\iff c_{\\text{index}}/c_{\\text{rerank}}<1-|C|/|U|\\). When \\(c_{\\text{index}}\\ll c_{\\text{rerank}}\\) and \\(|C|\\ll|U|\\) the right side \\(\\to 1\\) and the strict inequality holds. Relevance-monotonicity means every relevant \\(x\\) satisfies the test, so \\(x\\in C\\) for all relevant \\(x\\), giving recall@\\(C=1\\) (\\(\\approx 1\\) under non-ideal tests). \\(\\square\\)</p>\n\n<p><strong>In fabius.</strong> R9b governs archivum retrieval: run the cheap symbolic index/tag/keyword filter first to cut \\(U\\) to \\(C\\), then spend the expensive dense rerank only on \\(C\\) — provably cheaper whenever the symbolic test is near-free and selective, and lossless in recall when it is relevance-monotone.</p>",
  "class": "real-math",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "R9c",
  "title": "Summarize-then-link is rate–distortion with lossless recovery",
  "html": "<p><strong>Statement.</strong> Let \\(S\\) be the source page (a random variable over a finite alphabet \\(\\mathcal{S}\\)) and let \\(\\hat{S}\\) be an inlined summary drawn through a channel \\(p(\\hat{s}\\mid s)\\). Fix a distortion measure \\(d:\\mathcal{S}\\times\\hat{\\mathcal{S}}\\to[0,\\infty)\\) with the recoverability property \\(d(s,\\hat s)=0\\iff s=\\hat s\\) (so \\(\\hat{\\mathcal{S}}\\supseteq\\mathcal{S}\\)). The rate–distortion function is \\[ R(D)=\\min_{\\,p(\\hat{s}\\mid s)\\,:\\,\\mathbb{E}[d(S,\\hat{S})]\\le D}\\; I(S;\\hat{S}). \\] Assume (i) \\(\\mathcal{S}\\) is nondegenerate, so \\(H(S)>0\\); (ii) the inlined view has a hard rate budget \\(B<H(S)\\) bits per source symbol. Claim: any inlined view operating at mutual information \\(\\le B\\) incurs distortion at least \\(D(B)>0\\), where \\(D(B)=\\inf\\{D:R(D)\\le B\\}\\); yet the stored page reached through the link \\([[\\texttt{slug}]]\\) carries distortion \\(0\\).</p><p><strong>Proof.</strong> Under the recoverability property, \\(R(D)\\) is convex, nonincreasing and continuous on \\([0,D_{\\max}]\\), with \\(R(D)=0\\) for \\(D\\ge D_{\\max}=\\min_{\\hat{s}}\\mathbb{E}[d(S,\\hat s)]\\) and \\(R(0)=I(S;S)=H(S)\\) (discrete, finite-alphabet case). An inlined view with \\(I(S;\\hat S)\\le B\\) is constrained accordingly. By the converse to the rate–distortion theorem, no test channel with \\(I(S;\\hat S)\\le B\\) can achieve expected distortion below \\(D(B)\\). Since \\(B<H(S)=R(0)\\) and \\(R\\) is strictly decreasing on the interval where it is positive (so its inverse \\(D(\\cdot)\\) is well-defined there), \\(R(D)=B<R(0)\\) forces \\(D(B)>0\\). Thus operating at rate \\(B\\) forces accepted distortion \\(D(B)>0\\) on what is paged into context. Now consider storage. The summary is emitted with a pointer \\([[\\texttt{slug}]]\\) resolving to the verbatim page. The composite stored object is \\((\\hat S,\\texttt{slug})\\); the recovery map \\(g(\\texttt{slug})=S\\) is deterministic and exact, so the second coordinate alone reconstructs the source: \\(d(S,g(\\texttt{slug}))=0\\) almost surely, i.e. the stored record realizes the point \\((D{=}0,R{=}H(S))\\) on the curve. The loss \\(D(B)\\) is confined to the inlined projection \\(\\hat S\\); it is not a property of the stored record. Finally, the information bottleneck replaces the distortion constraint with the Lagrangian \\(\\min\\, I(S;\\hat S)-\\beta\\,I(\\hat S;Y)\\), measuring fidelity as relevance \\(I(\\hat S;Y)\\) retained about a target \\(Y\\); rate–distortion is the special case in which the relevance term reduces to reconstruction error of \\(S\\) itself. So summarize-then-link is the \\(\\beta\\)-weighted bottleneck with \\(Y=\\) the query the page must still answer: the inline view trades rate for relevance, the linked page restores full \\(I(S;Y)\\). \\(\\square\\)</p><p><strong>In fabius.</strong> When a write would exceed the context/token budget \\(B\\), archivum does not truncate destructively: it inlines a budget-fitted summary (accepting \\(D(B)\\)) and emits a \\([[\\texttt{slug}]]\\) to the full page, so the paged-in view is lossy but the stored record is recoverable at zero distortion.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Recoverability property d(s,s_hat)=0 iff s=s_hat was used to assert R(0)=H(S) but only stated parenthetically mid-proof; promoted it into the Statement's hypotheses where it is load-bearing, and noted hat-S alphabet must contain S for zero distortion to be achievable.",
   "Source alphabet should be finite/discrete for R(0)=H(S) and continuity to hold cleanly; the original said 'random variable over messages' loosely. Pinned to finite alphabet so the converse, convexity, continuity and R(0)=H(S) are all valid (in general/continuous sources R(0) need not equal H(S)).",
   "The budget B is a per-symbol RATE (bits per source symbol), the natural argument of R; original wavered between 'tokens'/'bits'. Stated B as a rate budget so 'I(S;hat S)<=B' is dimensionally the right constraint feeding R(D); the 'In fabius' application keeps the informal token reading, which is fine operationally.",
   "Strict monotonicity claim: R is strictly decreasing only on the region where R>0 (it is flat at 0 for D>=D_max). Original wrote 'strictly decreasing on [0,D_max]'. Corrected to 'strictly decreasing where it is positive' so the inversion D(B) is justified exactly on the relevant range, which is where B<R(0) places it.",
   "No overstatement of the analogy: the information-bottleneck paragraph correctly frames RD as a special case and does not claim it proves anything beyond the transmitted/stored decomposition; left intact with tightened wording."
  ]
 },
 {
  "rule": "M8",
  "title": "Rank by probability of relevance (PRP)",
  "html": "<p><strong>Statement.</strong> Fix a query \\(q\\) and a finite set of index pages \\(\\{p_1,\\dots,p_n\\}\\). For each page let \\(\\pi_i := P(\\text{relevant}\\mid q, p_i)\\), and assume (i) relevance judgements are made <em>independently</em> per page given \\(q\\) (so the relevance indicators \\(R_i\\in\\{0,1\\}\\) have \\(\\mathbb{E}[R_i]=\\pi_i\\) and the value of a page does not depend on which other pages are surfaced), and (ii) <em>uniform retrieval cost</em> — every page costs the same to inspect, so a ranking is just a permutation and the only choice is order. Let a ranking present pages in order \\(\\sigma(1),\\dots,\\sigma(n)\\). Then ranking by descending \\(\\pi_i\\) maximizes the expected number of relevant pages among the top \\(k\\), for <em>every</em> cutoff \\(k\\) simultaneously; hence it maximizes expected precision \\(P@k\\) and expected recall \\(R@k\\) at every \\(k\\), and is Bayes-optimal.</p>\n<p><strong>Proof.</strong> The expected count of relevant pages in the top \\(k\\) is, by linearity of expectation (no independence needed for this step), \\(\\mathbb{E}[\\#\\text{rel}@k]=\\sum_{j=1}^{k}\\pi_{\\sigma(j)}\\). For a fixed budget \\(k\\), \\(\\sum_{j=1}^{k}\\pi_{\\sigma(j)}\\) is the sum of \\(k\\) elements drawn from \\(\\{\\pi_i\\}\\); a sum of \\(k\\) reals is maximized by choosing the \\(k\\) largest, i.e. by placing the top-\\(\\pi\\) pages first. The descending-\\(\\pi\\) permutation \\(\\sigma^\\star\\) puts the \\(k\\) largest \\(\\pi\\) in positions \\(1..k\\) <em>for every \\(k\\) at once</em> (a prefix of a sorted list is its own top-\\(k\\)), so it is simultaneously optimal at all cutoffs. Then \\(\\mathbb{E}[P@k]=\\tfrac1k\\sum_{j\\le k}\\pi_{\\sigma(j)}\\) and \\(\\mathbb{E}[R@k]=\\tfrac1{\\,\\mathbb{E}[\\#\\text{rel}]\\,}\\sum_{j\\le k}\\pi_{\\sigma(j)}\\), where \\(\\mathbb{E}[\\#\\text{rel}]=\\sum_i\\pi_i\\) is constant in \\(\\sigma\\); each is a monotone-increasing function of the same maximized prefix sum, so both are maximized by \\(\\sigma^\\star\\). What licenses \"pick the \\(k\\) largest\" is the additive form of the objective, which comes from <em>linearity of expectation alone</em>. The role of <em>independence</em> is upstream of that: it guarantees each page's contribution is its own \\(\\pi_i\\) with no inter-document coupling — no page's relevance value is altered by, or redundant with, the others already surfaced — so the per-page scores \\(\\pi_i\\) are exactly the marginal contributions being summed. (When relevance is correlated — e.g. near-duplicate or novelty effects — this marginal-equals-total identification fails and greedy-by-\\(\\pi\\) can be beaten, the classical limit of PRP.) Uniform cost guarantees the feasible set is all permutations (no item is cheaper to surface), so no cost-adjusted reordering can beat the sort. This is the probability-ranking principle; optimality of the posterior-mean ordering is Bayes-optimality. <strong>Utility generalization.</strong> Let load-bearingness \\(u(p_i)\\ge0\\) weight each page. Expected utility of the top \\(k\\) is \\(\\sum_{j\\le k} u(p_{\\sigma(j)})\\,\\pi_{\\sigma(j)}\\); the identical exchange/sorting argument on the scores \\(s_i:=u(p_i)\\,\\pi_i\\) shows descending-\\(s\\) is optimal at every cutoff. \\(\\square\\)</p>\n<p><strong>In fabius.</strong> M8 ranks archivum index entries by \\(P(\\text{relevant}\\mid q,\\text{page})\\) and retrieves the top few; when pages differ in load-bearingness it ranks by \\(s=u(\\text{page})\\cdot P(\\text{relevant}\\mid q,\\text{page})\\), with \\(u\\) entering strictly as an outer utility weight — never folded into the bare relevance probability that PRP optimizes.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Incorrect attribution: the proof claimed independence 'licenses pick the k largest' by making the objective decompose additively. The additive decomposition follows from linearity of expectation alone (the proof even says 'no independence needed yet'), not from independence. Fixed by attributing additivity to linearity and reassigning independence to its true role: identifying each pi as the page's marginal, non-redundant contribution (the assumption whose failure under correlated/duplicate relevance is the known PRP breakdown).",
   "Assumption (i) originally stated only E[R_i]=pi_i, which is a marginal condition that does not by itself capture the no-inter-document-coupling content that PRP actually needs; strengthened the wording of (i) to state that a page's value does not depend on which other pages are surfaced.",
   "Minor: made explicit that the recall normalizer E[#rel]=sum_i pi_i is constant in sigma, which is what makes maximizing the prefix sum maximize expected recall."
  ]
 },
 {
  "rule": "M8c",
  "title": "Synthesize logs only when compression pays off",
  "html": "<p><strong>Statement.</strong> Let \\(L=(l_1,\\dots,l_n)\\) be a batch of log lines drawn from a countable line-alphabet \\(\\mathcal{A}\\). Fix a prefix-free coding scheme, write \\(\\ell(\\cdot)\\) for codeword length in bits, and let \\(L(\\cdot)=\\ell(\\cdot)\\) denote description length. A <em>synthesis page</em> is a model \\(p\\) (a hypothesis/dictionary) under which the batch is re-encoded, giving a two-part code of length \\(L(p)+L(L\\mid p)\\), where \\(L(L\\mid p)\\) is the length of \\(L\\) encoded relative to \\(p\\). The <em>raw</em> code stores each line independently at cost \\(\\sum_{i=1}^n L(l_i)\\). Assume both codes are uniquely decodable (each is a valid prefix code, so each satisfies the prefix-code inequality: \\(\\sum 2^{-\\ell}\\le 1\\)). <strong>Claim.</strong> Under a shortest-total-description criterion one should adopt the synthesis page exactly when</p>\n\\[ L(p) + L(L \\mid p) \\;<\\; \\sum_{i=1}^{n} L(l_i). \\]\n\n<p><strong>Proof.</strong> The description-length criterion selects, among admissible descriptions of the data, the one of minimum total length — the operational reading of \"the best hypothesis compresses best,\" itself a computable proxy for algorithmic complexity \\(K\\), which is uncomputable but satisfies \\(K(x)\\le \\ell(x)+O(1)\\) for every computable prefix code and so lower-bounds each. Both candidates are admissible descriptions of the <em>same</em> object \\(L\\): by unique decodability each lets a receiver reconstruct \\(l_1,\\dots,l_n\\) exactly, so comparing their lengths is comparing two lossless encodings of one message. The decision rule is therefore the total order on lengths: prefer \\(p\\) iff \\(L(p)+L(L\\mid p) < \\sum_i L(l_i)\\), which is the claim. The content of the inequality is structural. The raw cost \\(\\sum_i L(l_i)\\) pays full price per line. The two-part cost splits into <em>model cost</em> \\(L(p)\\) — paid once — and <em>residual</em> \\(L(L\\mid p)=\\sum_i L(l_i\\mid p)\\), the lines re-encoded against the shared regularities \\(p\\) captures. Define the <em>redundancy</em> \\(R=\\sum_i\\big(L(l_i)-L(l_i\\mid p)\\big)\\), the net bits saved across lines by encoding through \\(p\\). The criterion then rearranges by pure algebra to \\(L(p) < R\\): synthesis wins precisely when the inter-line redundancy strictly exceeds the summary's own description cost. Note \\(R\\) need not be nonnegative — for a poorly fitted or mis-specified \\(p\\) a conditional codeword may be <em>longer</em> than the unconditional one on these particular realized lines, since pointwise codeword length is not bounded by entropy (the inequality \\(H(l_i\\mid p)\\le H(l_i)\\) governs only <em>expected</em> optimal length, not the length of a fixed outcome). When \\(R\\le 0\\) the residual already matches or exceeds the raw cost and the criterion keeps the lines regardless of \\(L(p)\\); when \\(R>0\\) but \\(R\\le L(p)\\) the savings fail to amortize the summary, and again raw wins. Synthesis is justified exactly at \\(R>L(p)\\), and not before. \\(\\square\\)</p>\n\n<p><strong>In fabius.</strong> archivum folds a grown batch of log lines into a synthesis page only when the page's description cost \\(L(p)\\) is strictly paid back by net cross-line redundancy \\(R=\\sum_i(L(l_i)-L(l_i\\mid p))>L(p)\\); until the log accumulates enough genuinely shared structure to make that two-part code shorter, the raw lines stay, so synthesis fires precisely at the description-length break-even and never on a still-incompressible log.</p>",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Entropy-to-pointwise confusion: the proof claimed 'By Shannon's source-coding bound the achievable residual per line is governed by H(l_i|p) <= H(l_i)... exploiting redundancy can only shorten, never lengthen, the optimal per-line code.' The entropy inequality H(l_i|p) <= H(l_i) is about EXPECTED optimal code length, not the realized codeword length L(l_i|p) of a specific fixed line. For an individual outcome (and for a mis-specified p) a conditional code can be strictly longer. The argument does not establish a per-line shortening.",
   "Unsupported sign claim R >= 0: the proof asserted the redundancy R = sum(L(l_i) - L(l_i|p)) >= 0 and derived it from the (inapplicable) entropy inequality. R is not guaranteed nonnegative for realized lengths or a bad p. This is over-asserted and not load-bearing — MDL requires L(p) < R, not R >= 0. Corrected to note R may be negative and that raw wins whenever R <= L(p) (covering both R<=0 and 0<R<=L(p)).",
   "Tightened the Kolmogorov-complexity remark to the precise statement K(x) <= l(x) + O(1) (K lower-bounds each computable prefix code up to a constant), replacing the loose 'lower-bounds every prefix code'.",
   "Preserved the correct results: the MDL decision rule L(p)+L(L|p) < sum L(l_i) and its algebraic rearrangement to L(p) < R are exact and retained; the 'In fabius' mapping was made consistent with the two-sided R (synthesis iff R > L(p))."
  ]
 },
 {
  "rule": "R4",
  "title": "Scout wide: hold candidate routes until the signal sharpens",
  "html": "<p><strong>Rule.</strong> On an ambiguous task, fabius keeps two or three candidate routes alive and commits to one only as the signal sharpens — scout wide, then strike narrow. It holds the alternatives as a weighted preference over routes rather than guessing a single route early, and collapses to one once the integrated evidence separates them.</p>\n<p><strong>Why it holds.</strong> This is an operational heuristic, not a theorem: an early hard commit under ambiguity risks the wrong route with no recovery, while carrying a small set of routes costs little and lets cheap early signal decide. It carries no objective function and never overrides a value-of-information gate — it only defers the collapse until the gate can fire on evidence. It is fabius's own maxim, scout wide / strike narrow, applied to routing.</p>",
  "class": "qualitative",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "R10",
  "title": "State breadth tasks once; hard-steer only narrow contracts",
  "html": "<p><strong>Rule.</strong> fabius states a breadth task once and lets the specialist range; it hard-steers only a narrow contract. Instruction emphasis is a dial — turn it up to force adherence to a hard constraint, keep it modest when the breadth of candidate outputs matters — and it measures both adherence and breadth rather than maxing emphasis blindly.</p>\n<p><strong>Why it holds.</strong> This is an operational heuristic, not a theorem: over-steering a task that wanted range collapses the output to a narrow mode; under-steering a task with a hard constraint lets it drift. The honest, verifiable core is directional — raising emphasis raises adherence and lowers breadth — so fabius tunes emphasis to the task's kind and checks the trade rather than assuming a fixed optimum.</p>",
  "class": "qualitative",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "M8b",
  "title": "Break index ties by recency — an order-only tie-break",
  "html": "<p><strong>Rule.</strong> When archivum's M8 retrieval leaves a relevance tie on a curated wiki, fabius breaks it toward the more recently edited page — a deterministic recency tie-break, applied only after relevance and never as a scoring stage that can override it.</p>\n<p><strong>Why it holds.</strong> This is an operational heuristic, not a posterior: on a tie, recency is a cheap, monotone, order-only signal (more recent first) that carries no probabilistic claim and cannot outrank relevance. fabius keeps only the monotone tie-break and deliberately drops any fitted decay rate or importance weighting, so no claim of optimality or calibration is made or needed.</p>",
  "class": "qualitative",
  "verdict": "correct",
  "issues": []
 },
 {
  "rule": "R11",
  "title": "The tier cascade is an index policy: dispatch at cost-per-success, escalate on a verified miss",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Lemma 2 claimed 'weakly better iff rho_i ≤ rho_k'; the only-if direction fails when q = 0 (unreachable slot: both orders cost the same even with rho_i strictly larger). Restated as 'if', an equivalence only when q is positive.",
   "The Statement claimed cheapest-first is optimal 'precisely when' the price order agrees with the rho-order; the strict-suboptimality converse requires the inverted slot to be reachable (Theorem (b) states this, the Statement dropped it). Reworded to 'whenever ... / strictly suboptimal whenever a reachable cheap tier ...'.",
   "The Statement's gate read as a participation rule ('attempt tier i iff p_i W exceeds c_i'), but inside the equal-guarantee class participation is mandatory — the gate prices ORDER (prepend), not whether a tier runs; a gate that truncates the cascade changes the success guarantee and exits the class. Clarified in the Statement and the threshold-form paragraph; truncation optimality explicitly not claimed.",
   "Corollary (i) cited Theorem (b) for an exchange on an arbitrary session-wide order and silently skipped the step from a pairwise rho-inversion to an adjacent strict inversion; the step is now spelled out and attributed to Lemma 2, where it belongs.",
   "The Pandora attribution was loose: only the ordering half of Weitzman's rule applies here — the stopping half (abandon to the outside option) is pinned shut by the equal-guarantee constraint. Tightened.",
   "Corollary (iii)'s domination prices a hunch-escalated attempt at the full c_j with verification folded in (A2); a policy that skips verification precisely to recoup that cost sits outside the model and is NOT dominated by the argument. Added to the Boundary so the result is not overread.",
   "Corollary (iii)'s dual was phrased as if the policy conditions on the unobserved true outcome ('stopping unverified ... when the attempt in fact failed'); reworded as a blind stop — the loss formula q_j alpha (1−p_j) S_{j+1} was already correct for the blind-stop model.",
   "Terminology error with a numeric consequence: the entry said \"odds\" where the mathematics is about probability. The anchor case proves p_1 > c_1/c_2 = 10% (a probability), but stated it as \"verified success odds exceed 10%\"; under the standard reading odds = p/(1-p), odds of 10% means p ≈ 9.09%, contradicting the proved threshold. Same misuse in the Statement's gloss of rho (\"price per unit success odds\"), corollary (i), the Boundary's correlation discussion, and the placement string. Fixed by normalizing all quantitative uses to \"probability\".",
   "Corollary (iii), hunch-accept dual: the claim that blind acceptance \"exits the equal-guarantee class\" requires S_{j+1} > 0, which was never justified and is false at the final slot (empty continuation gives S_{K+1} = 0 — there a blind accept does not lower in-cascade success probability; it swallows the fallback instead, violating the class's stopping rule for a different reason). The escalation half had scoped itself to nonempty continuation and proved W_{j+1} > 0, but the dual half did not. Fixed by making the same-slot scope explicit, adding the positivity argument (nonempty continuation and every p_i > 0), conditioning the strict exit on slot reachability, and adding the final-slot edge case.",
   "Everything else survived adversarial verification: the expected-cost formula, Lemma 1's adaptivity collapse (only observable on the non-terminated path is a string of fails, so deterministic-adaptive traces one permutation; randomized is a mixture), Lemma 2's exchange identity including the q = 0 and tie edge cases, the bubble-sort termination and equal-rho block argument, the converse's strictness and its reachability hypothesis, the threshold algebra c_i + (1-p_i)W < W iff W > rho_i, the EVOI variable mapping, the c-tilde = c_i + (1-p_i)d_i damage substitution (linearity checked), the hunch-escalation excess q_j·beta·p_j·W_{j+1}, and the hunch-accept drop q_j·alpha·(1-p_j)·S_{j+1} — all confirmed both by hand and by brute-force enumeration over all K! permutations on randomized instances (scripts at a local scratch verification script (not included in this repository)). The \"real\" status tag is honest: this is a genuine optimal-sequencing theorem (Mitten's c/p index; the ordering half of Pandora at Bernoulli boxes) that governs the dispatch decision, with the load-bearing independence and calibration assumptions correctly quarantined in the Boundary. No literal < or > appears in any of the 134 math regions."
  ],
  "html": "<p><strong>Statement.</strong> Route a sub-task down a ladder of model tiers ordered by price, escalating only when the R8 verifier certifies a miss. <strong>Claim.</strong> Among all dispatch policies that keep attempting untried tiers until the first verified success — a class in which every policy achieves the identical success guarantee — expected spend is minimized by the static cascade sorted by the reservation index \\(\\rho_i := c_i/p_i\\) (price per unit of success probability), and each dispatch decision is one expected-loss threshold of exactly R3's form: attempt tier \\(i\\) ahead of the rest of the plan iff the spend it stands to cancel exceeds its price, \\(p_i W \\gt c_i\\). Cheapest-first is the optimal cascade whenever the price order agrees with the \\(\\rho\\)-order — which is what the operational clause \"the cheapest tier that holds\" enforces — and it is strictly suboptimal whenever a reachable cheap tier's success probability is too thin to earn its slot, which is why ambiguity, architecture, security and irreversible actions route straight to the strong tier by the same index, not by exception.</p>\n<p><strong>Setting.</strong> Fix a sub-task \\(t\\). Tiers \\(i\\in\\{1,\\dots,K\\}\\) have attempt costs \\(0 \\lt c_1 \\le c_2 \\le \\dots \\le c_K\\) (each \\(c_i\\) includes that attempt's R8 verification cost) and per-sub-task success probabilities \\(p_i := p_i(t) \\in (0,1]\\); attempt outcomes are independent across tiers. The verifier is sound and complete: an attempt passes iff it genuinely clears the sub-task's bar. A <em>policy</em> attempts untried tiers one at a time — possibly adaptively, possibly at random — stopping at the first pass; if all \\(K\\) tiers fail it pays a fixed fallback \\(F \\ge 0\\) (human escalation, R7/M2). We compare policies in the <em>equal-guarantee class</em>: no policy invokes the fallback while an untried tier remains. Every policy in the class succeeds with probability \\(1-\\prod_{i=1}^{K}(1-p_i)\\) — the guarantee is order-invariant, so ordering affects only spend. For a static order (permutation) \\(\\sigma\\),\n\\[\\mathbb{E}[C(\\sigma)] \\;=\\; \\sum_{j=1}^{K} c_{\\sigma(j)}\\prod_{l=1}^{j-1}\\bigl(1-p_{\\sigma(l)}\\bigr)\\;+\\;F\\prod_{l=1}^{K}\\bigl(1-p_{\\sigma(l)}\\bigr),\\]\nsince tier \\(\\sigma(j)\\) is paid iff the first \\(j-1\\) attempts all failed. The \\(F\\)-term is permutation-invariant and drops out of every comparison below.</p>\n<p><strong>Lemma 1 (adaptivity collapses to a static order).</strong> The minimum of expected spend over all policies in the class equals the minimum over the \\(K!\\) static orders. <em>Proof.</em> The only information a policy ever observes is the verifier's verdict, and a pass terminates the cascade. Hence along the unique non-terminated path every verdict is \"fail\", so a deterministic adaptive policy's successive choices are all made on that one history and trace one fixed permutation: deterministic-adaptive equals static. A randomized policy is a probability mixture over deterministic ones, and its expected spend is the corresponding convex combination of finitely many static-order spends, which is minimized at a pure permutation. \\(\\square\\)</p>\n<p><strong>Lemma 2 (exchange).</strong> Let \\(\\sigma\\) be any order and \\(\\sigma'\\) the same order with adjacent slots \\(j,j+1\\) transposed; write \\(i=\\sigma(j)\\), \\(k=\\sigma(j+1)\\), and \\(q=\\prod_{l \\lt j}\\bigl(1-p_{\\sigma(l)}\\bigr)\\) for the probability of reaching slot \\(j\\). Then\n\\[\\mathbb{E}[C(\\sigma)]-\\mathbb{E}[C(\\sigma')] \\;=\\; q\\,\\bigl(p_k c_i - p_i c_k\\bigr)\\;=\\;q\\,p_i p_k\\,(\\rho_i-\\rho_k),\\]\nso placing \\(i\\) before \\(k\\) is weakly better if \\(\\rho_i \\le \\rho_k\\) (an equivalence when \\(q \\gt 0\\); at \\(q = 0\\) the two orders cost the same), and strictly better iff \\(\\rho_i \\lt \\rho_k\\) and \\(q \\gt 0\\). <em>Proof.</em> Slots before \\(j\\) contribute identically. The pair contributes \\(q\\,[c_i+(1-p_i)c_k]\\) under \\(\\sigma\\) versus \\(q\\,[c_k+(1-p_k)c_i]\\) under \\(\\sigma'\\). Slots after \\(j+1\\) contribute identically: they are reached with probability \\(q(1-p_i)(1-p_k)\\) in either order — symmetric in \\(i,k\\) — with the same set of tried tiers, hence identical continuations. Subtracting the pair terms gives \\(q(p_k c_i - p_i c_k)\\); dividing by \\(p_i p_k \\gt 0\\) shows its sign is that of \\(\\rho_i - \\rho_k\\). \\(\\square\\)</p>\n<p><strong>Theorem (index-cascade optimality).</strong> (a) Any static order nondecreasing in \\(\\rho\\) minimizes expected spend over the entire equal-guarantee class, and all \\(\\rho\\)-nondecreasing orders have equal spend. (b) In particular the cheapest-first cascade \\(1,2,\\dots,K\\) is optimal whenever \\(\\rho_1\\le\\rho_2\\le\\dots\\le\\rho_K\\); conversely, if \\(\\rho_j \\gt \\rho_{j+1}\\) for some \\(j\\) and slot \\(j\\) is reached with positive probability (\\(p_l \\lt 1\\) for all \\(l \\lt j\\)), cheapest-first is strictly suboptimal — the exchange strictly saves.</p>\n<p><strong>Proof.</strong> (a) Take any permutation \\(\\sigma\\). While some adjacent pair satisfies \\(\\rho_{\\sigma(j)} \\gt \\rho_{\\sigma(j+1)}\\), transpose it: by Lemma 2 the expected spend weakly decreases, and the transposition changes the relative order of that pair only, so the count of strict \\(\\rho\\)-inversions falls by exactly one. The procedure therefore terminates, at an order with no adjacent strict inversion — i.e. a \\(\\rho\\)-nondecreasing order — whose spend is at most \\(\\mathbb{E}[C(\\sigma)]\\). Hence some sorted order attains the minimum over permutations, and by Lemma 1 over the whole class. Any two \\(\\rho\\)-nondecreasing orders can differ only in the arrangement of equal-\\(\\rho\\) tiers, which occupy the same consecutive block of slots in both (the block is fixed by the counts of strictly smaller and equal index values); within a block any rearrangement is a product of adjacent transpositions of equal-\\(\\rho\\) pairs, each changing spend by \\(q\\,p_ip_k(\\rho_i-\\rho_k)=0\\) by Lemma 2. So all sorted orders cost the same. (b) The first claim is a case of (a). For the converse, apply Lemma 2 to the identity order at slots \\(j,j+1\\): the swap changes spend by \\(q\\,p_jp_{j+1}(\\rho_j-\\rho_{j+1}) \\gt 0\\) since \\(q \\gt 0\\), so the identity is not optimal. \\(\\square\\)</p>\n<p><strong>Threshold form (the gate the coherence extension composes).</strong> The theorem compresses to one scalar gate. Let \\(W\\) be the expected spend of any plan for the sub-task that does not attempt tier \\(i\\) (the rest of the cascade, or the strong tier directly). Prepending a verified attempt of tier \\(i\\) costs \\(c_i\\) and with probability \\(p_i\\) cancels \\(W\\) entirely, so the plan-with-probe spends \\(c_i+(1-p_i)W\\). Prepend iff\n\\[c_i + (1-p_i)\\,W \\;\\lt\\; W \\;\\iff\\; p_i W \\gt c_i \\;\\iff\\; W \\gt \\rho_i.\\]\nThis is R3's gate verbatim, on disjoint variables: \\(a = W\\) (expected spend acting without the probe), \\(b = (1-p_i)W\\) (expected residual spend after it), \\(\\mathrm{EVOI} = a-b = p_iW\\), \\(c_{\\text{call}} = c_i\\), and the gate fires iff \\(\\mathrm{EVOI} \\gt c_{\\text{call}}\\). Equivalently \\(\\rho_i\\) is tier \\(i\\)'s <em>reservation value</em> — the downstream-spend level at which attempting it exactly breaks even — and the \\(\\rho\\)-sorted cascade is the greedy reservation-value policy (equivalently, the reservation-value ordering; the \\(c/p\\) exchange index is the sequential-testing index). The gate prices <em>where</em> a tier goes, not <em>whether</em> it runs: participation is pinned by the equal-guarantee class, and letting the same gate also truncate the cascade — skip tier \\(i\\) straight to the fallback when \\(W \\le \\rho_i\\) — changes the success guarantee and exits the class, so no optimality is claimed for truncation here. Anchor case \\(K=2\\), \\(p_2=1\\): try the cheap tier first iff \\(p_1 \\gt c_1/c_2\\) — at a tenfold price gap the cheap tier earns its slot iff its verified success probability exceeds \\(10\\%\\).</p>\n<p><strong>Corollaries — one per clause of the shipped rule.</strong> (i) <em>Re-tier per sub-task.</em> The index is \\(\\rho_i(t)=c_i/p_i(t)\\): prices are global but success probabilities are per-sub-task. If sub-tasks \\(t,t'\\) order two tiers oppositely (\\(\\rho_i(t) \\lt \\rho_k(t)\\) but \\(\\rho_i(t') \\gt \\rho_k(t')\\)), any single session-wide order inverts the pair \\(\\{i,k\\}\\) for one of them, hence is not \\(\\rho\\)-sorted for that sub-task; a non-sorted order has an adjacent strict inversion, and when that slot is reachable the exchange of Lemma 2 strictly saves — the session-wide order strictly overspends there. (ii) <em>Reserve the strong tier — by index, not exception.</em> On ambiguity, architecture and security sub-tasks \\(p_{\\text{cheap}}(t)\\) is small, so \\(\\rho_{\\text{cheap}} = c_{\\text{cheap}}/p_{\\text{cheap}}\\) blows up past \\(\\rho_{\\text{strong}}\\) and the sorted cascade opens with the strong tier: dispatching straight to strength on hard calls is the same index policy. For irreversible actions, let a tier-\\(i\\) miss additionally cause expected damage \\(d_i\\) before the verifier catches it; every occurrence of \\(c_i\\) in the spend formula and in Lemma 2 becomes the expected outlay \\(\\tilde c_i = c_i + (1-p_i)d_i\\), the entire argument runs unchanged, and the inflated index \\(\\tilde\\rho_i=\\tilde c_i/p_i\\) pushes damage-prone cheap tiers behind the strong one. (iii) <em>Escalate on a verified miss, never a hunch.</em> Modify the optimal cascade so that at a slot \\(j\\) with nonempty continuation the agent, with probability \\(\\beta \\gt 0\\), escalates without consulting the verifier. With probability \\(q_j\\,\\beta\\,p_j\\) a genuine success is discarded and the continuation spend \\(W_{j+1} \\gt 0\\) (positive since all \\(c_i \\gt 0\\)) is paid needlessly: the expected excess \\(q_j\\,\\beta\\,p_j\\,W_{j+1} \\gt 0\\) whenever slot \\(j\\) is reachable, and the discarded success is recovered only if a later tier passes, so the guarantee cannot rise — hunch-escalation is dominated on both coordinates. Dually, <em>accepting</em> on a hunch at the same slot \\(j\\) — stopping unverified with probability \\(\\alpha \\gt 0\\), blind to the attempt's true outcome — costs nothing when the attempt happened to succeed but strands the sub-task when it failed: it lowers the success probability by \\(q_j\\,\\alpha\\,(1-p_j)\\,S_{j+1}\\), where \\(S_{j+1}\\) is the continuation's success probability — positive here, since the continuation is nonempty and every \\(p_i \\gt 0\\) — and so exits the equal-guarantee class altogether whenever slot \\(j\\) is reachable. (At the final slot the continuation is empty and \\(S_{K+1}=0\\); there a blind accept instead swallows the fallback \\(F\\), reporting an unverified outcome as done — it violates the class's stopping rule directly rather than through the guarantee.) Hence verified-miss escalation is the only admissible trigger in the class, and among admissible policies the Theorem selects the index cascade. \\(\\square\\)</p>\n<p><strong>In fabius.</strong> R11 dispatches each sub-task to the \\(\\rho\\)-minimal tier and lets R8's verdict — never a vibe — trigger the climb. \"The cheapest tier that holds\" is exactly the condition that the price order and the \\(\\rho\\)-order coincide: mechanical, tightly-contracted work has \\(p_{\\text{cheap}}\\approx 1\\), so \\(\\rho_{\\text{cheap}}\\approx c_{\\text{cheap}}\\) and stays cheap, while ambiguity, security calls and irreversibility inflate \\(\\rho_{\\text{cheap}}\\) (through small \\(p\\) or damage \\(d\\)) and route to strength via the same single threshold \\(W \\gt \\rho_i\\) that R3 applies to calls and R2 applies to ladder rungs.</p>\n<p><strong>Boundary.</strong> PROVED, under the stated model: Lemmas 1–2, the Theorem, the threshold/EVOI form, and corollaries (i)–(iii) — assuming (A1) attempt outcomes independent across tiers, (A2) a sound and complete verifier with its cost folded into \\(c_i\\), (A3) known, calibrated \\((c_i, p_i(t))\\), (A4) risk-neutral expected spend with one attempt per tier. What remains operational heuristic: independence (A1) is load-bearing — real tier failures correlate through latent sub-task difficulty, a cheap-tier miss is evidence lowering every untried tier's posterior success probability, and under correlation the static index policy loses its optimality proof (recomputing indices on posterior probabilities is the sensible move, but no optimality is claimed here, and optimal correlated sequencing is intractable in general); same-tier retries are the maximally correlated case and sit outside the model. Corollary (iii) prices a hunch-escalated attempt at the full \\(c_j\\) — verification folded in per (A2) — so a policy that skips verification precisely to recoup its cost sits outside the model and is not dominated by this argument; it is excluded on other grounds (an unverified pass certifies nothing). R8's real verifier is only approximately sound and complete — a false pass erodes the \"same success guarantee\" clause at a rate this entry does not bound. The indices themselves are estimates: the theorem is conditional on calibration, and no robustness bound for misordered \\(\\rho\\)'s is proved. Which sub-tasks count as \"ambiguity / architecture / security\" is a classification judgment about where \\(p_{\\text{cheap}}\\) is small, not a theorem. Finally, headline savings figures are task- and dataset-specific; what transfers — and what is proved — is the index/threshold form, not any particular cost number.</p>"
 },
 {
  "rule": "R12",
  "title": "Dual exit gate + hard cap: the long-horizon step→verify loop as optimal stopping",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Dependence factor claimed '1 <= kappa' in general — false: the Fréchet lower bound permits kappa < 1 under negative dependence. Fixed to the full Fréchet range with positive dependence (shared source) identified as the failure direction that climbs toward the min bound.",
   "Claim that the martingale regime 'produces flat windows at rates the null cannot' is false in general — a nonnegative martingale can show flat windows with probability below L^w. Fixed: the isometry fires the window with probability 1, the martingale regime contains the isometry so admits no bound below 1; the test is now honestly framed as size <= L^w under the null, power 1 against the stalled-at-fixed-distance alternative, unquantified against slow leaks.",
   "'Contraction reaches e_k <= tol within ceil(...) cycles' overstates a stochastic (expectation) contraction — only E[e_k] decays geometrically; sample-path arrival needs a Markov tail. Fixed to expectation + geometrically decaying tail bound.",
   "Escalation-folding parenthetical claimed 'the same algebra returns the same threshold form' — untrue without an extra hypothesis, since min is concave and the exact test is a value-of-information inequality. Fixed: explicit posterior-resolving-gate assumption yields q_k + (1-q_k)p_{k+1} > c_cyc/c_esc, the same form with a strictly stricter constant; otherwise only the VoI inequality holds (same safe direction). Boundary updated accordingly.",
   "'Unique undominated policy' fails at threshold ties (stop and continue tie at equality) and p_{k+1} is undefined on {q_k = 1}. Fixed: ties broken toward stopping (the safe tie-break), convention (1-q_k)p_{k+1} = 0 on {q_k = 1} added to the Setting.",
   "Reflection-test step 'posterior unchanged hence p_{k+1} = p_k' used a hidden assumption; made explicit that the next attempt is drawn by the same policy from the unchanged cause-posterior, hence has the same success law.",
   "Optimality telescoping silently assumed a constant per-cycle cost while the Setting allows c_cyc in [c_min, c_max]; scoped to constant cost with the F_j-measurable varying-cost generalization noted, and the Boundary now states the optimality claim's exact scope (declare-done terminal loss + constant per-cycle cost).",
   "Placement string contained a literal '>' ('p > c/V'); rewritten without angle brackets per house style.",
   "Theorem (iii) optimality claim now explicitly scoped to the declare-done terminal loss so the theorem statement matches what the proof establishes.",
   "Optimality claim was false at the cap boundary: 'no optimal rule stops off S' and 'any rule differing from it off ties is dominated' are contradicted by trajectories that never enter S by cycle N — at j=N the harness forces every rule, optimal ones included, to stop off S. Fixed by qualifying 'before the cap' and restating the optimum as first-crossing-or-cap.",
   "The folded-escalation parenthetical claimed 'the constant moves in the safe direction', implying the folded rule is uniformly stricter than the plain one; in fact the left side also gains the resolution term q_k, so under a posterior-resolving gate the folded rule can correctly continue where the plain rule stops (counterexample: q_k=0.5, c_esc/W=0.5, c_cyc/W=0.05 — plain test needs p>0.1, folded test q+(1-q)p>0.1 always passes). Fixed to state both changes explicitly and disclaim uniform strictness.",
   "The Boundary repeated the vague 'same safe direction' claim for the general value-of-information inequality and left ambiguous whether first-crossing optimality extends to the folded (escalate-inclusive) loss; fixed: the folded test is established at one-step lookahead only (closed form only under a posterior-resolving gate), and first-crossing optimality is proved for the declare-done loss alone.",
   "Three wording overstatements tightened: Statement (i) attributed the multiplicative false-alarm drop to 'separately sourced' (the operational proxy) where the proof requires conditional independence; Statement (ii)'s 'hypothesis test of size L^w against R8's contraction' could be read with contraction as the alternative rather than the null; and 'holding a run-level budget ε forces the per-cycle rate below ε/N' presented a sufficient condition from a union bound as a necessity.",
   "Verified sound and left untouched: the min/union/Fréchet bounds and the κ range endpoints (min/(αCαD)=1/max(αC,αD) checked); the Bayes-factor identity under two-sided conditional independence; the run-level union bound; deterministic termination, cost ≤ N·c_max, rollback ≤ r; the conditional-Markov stall-test size L^w (threshold e_k is F_k-measurable and positive, e_k=0 case handled) with power 1 against the fixed-distance stall and no sub-1 bound in the isometry/martingale regimes; the absorption identity (1-q_k)(1-p_{k+1}) via the tower property; both threshold algebras including the q_k=1 convention and the tie at equality; the posterior-resolving evaluation q+(1-q)p>c_cyc/c_esc (re-derived, correct); the telescoping optional-sampling argument with {σ>j}∈F_j and bounded increments on the finite horizon; and E[σ*]≤W/c_min plus the 1/m tail. No literal < or > appears inside math — all comparisons use \\lt/\\gt.",
   "2026-09-08 accuracy pass: An expected stopping-time scale is not a tail guarantee; label external enforcement and calibrated inputs as implementation requirements."
  ],
  "html": "<p><strong>Statement.</strong> For multi-cycle autonomous work, run the <code>fabius-disciplina</code> <code>step → verify</code> cycle as a stochastic loop with exactly three exits: <em>stop-done</em> at the first cycle where the completion condition \\(C_k\\) and an explicit, separately-sourced done-signal \\(D_k\\) hold simultaneously; <em>stop-escalate</em> on a certified stall; and a hard cap of \\(N\\) cycles, rate-limited and checkpointed. We prove three things. (i) The conjunctive gate \\(C_k\\wedge D_k\\) never has a higher false-stop rate than either signal alone, shrinks it <em>multiplicatively</em> when the two signals are conditionally independent — the design target that <em>separately sourced</em> operationalizes — and pays at worst an <em>additive</em> price in missed stops; over an \\(N\\)-cycle horizon exactly this shrinkage is what keeps the run-level false-stop probability bounded. (ii) The loop terminates surely with deterministically bounded cost and bounded state corruption, and both stall tests are inherited objects: the no-progress window is a hypothesis test whose null is R8's contraction and whose size is at most \\(L^w\\), and the repeated-cause reflection test is M4's redundancy lemma verbatim. (iii) The whole rule is one expected-loss threshold — continue iff \\((1-q_k)\\,p_{k+1} \\gt c_{\\mathrm{cyc}}/W\\), M4's threshold functional applied to the disjoint variable pair (completion belief, per-cycle hazard) — and with the hard cap making the horizon finite, under a monotone (diminishing-returns) hazard this myopic threshold is the <em>exact</em> optimal stopping rule for the shipped declare-done loss, not merely a heuristic.</p>\n<p><strong>Setting.</strong> Cycles \\(k=1,2,\\dots\\) are adapted to a filtration \\((\\mathcal F_k)\\). Let \\(T_k\\) be the event that the task is truly complete after cycle \\(k\\); completion is absorbing, \\(T_k\\subseteq T_{k+1}\\). Write \\(q_k=\\mathbb P(T_k\\mid\\mathcal F_k)\\) (completion belief) and \\(p_{k+1}=\\mathbb P(T_{k+1}\\mid \\neg T_k,\\ \\mathcal F_k)\\) (per-cycle hazard); on \\(\\{q_k=1\\}\\) the hazard is vacuous and we adopt the convention \\((1-q_k)p_{k+1}=0\\). Each cycle emits two stop signals: \\(C_k\\), the verify condition read from the objective verifier, and \\(D_k\\), an explicit done-signal from a distinct channel (sentinel marker, independent checker, human ack). Error rates, taken uniform across cycles: false alarms \\(\\alpha_C=\\mathbb P(C_k\\mid\\neg T_k)\\), \\(\\alpha_D=\\mathbb P(D_k\\mid\\neg T_k)\\); misses \\(\\beta_C=\\mathbb P(\\neg C_k\\mid T_k)\\), \\(\\beta_D=\\mathbb P(\\neg D_k\\mid T_k)\\). Costs: per-cycle cost \\(c_{\\mathrm{cyc}}\\in[c_{\\min},c_{\\max}]\\) with \\(c_{\\max}\\) enforced by the rate limit; \\(W\\gt 0\\) the loss of a false stop (handing off incomplete work as done); \\(c_{\\mathrm{esc}}\\in(0,W)\\) the cost of escalating. From R8 reuse the progress metric \\(e_k=d(x_k,x^{\\ast})\\ge 0\\), the distance of the working state to the verify condition's fixed point.</p>\n<p><strong>Theorem.</strong> (i) <em>Gate.</em> Unconditionally \\(\\mathbb P(C_k\\wedge D_k\\mid\\neg T_k)\\le\\min(\\alpha_C,\\alpha_D)\\). If \\(C_k\\perp D_k\\mid\\neg T_k\\), it equals \\(\\alpha_C\\alpha_D\\), strictly below the min whenever both rates lie in \\((0,1)\\); meanwhile \\(\\mathbb P(\\neg(C_k\\wedge D_k)\\mid T_k)\\le\\beta_C+\\beta_D\\). Over a run of at most \\(N\\) gate evaluations the run-level false-stop probability is at most \\(N\\alpha_C\\alpha_D\\) (dual, independent) versus \\(N\\alpha_C\\) (single). (ii) <em>Termination.</em> \\(\\tau=\\min(\\tau_{\\mathrm{gate}},\\tau_{\\mathrm{stall}},N)\\le N\\) surely; total cost \\(\\le N c_{\\max}\\); a runaway rolls back at most \\(r\\) cycles of state, \\(r\\) the checkpoint interval. If the loop still contracts in R8's sense (\\(\\mathbb E[e_{k+1}\\mid\\mathcal F_k]\\le L\\,e_k\\), \\(L\\lt 1\\)), a \\(w\\)-cycle no-progress window fires falsely with probability at most \\(L^w\\). (iii) <em>Threshold and optimality.</em> One-step lookahead yields the policy: continue iff \\((1-q_k)p_{k+1}\\gt c_{\\mathrm{cyc}}/W\\), with ties broken toward stopping — at equality the two actions tie exactly, and stopping is the safe tie-break; at stop, declare done iff \\(q_k\\gt 1-c_{\\mathrm{esc}}/W\\), else escalate. With horizon capped at \\(N\\), constant per-cycle cost, and the monotone hypothesis (once the one-step continue value is non-positive it stays non-positive a.s.), the first-crossing rule is exactly optimal among all stopping times for the declare-done terminal loss, and every optimal rule satisfies \\(\\mathbb E[\\sigma^{\\ast}]\\le W/c_{\\min}\\).</p>\n<p><strong>Proof.</strong> <em>(i) False stops multiply down; misses add up.</em> \\(\\{C\\wedge D\\}\\subseteq\\{C\\}\\) and \\(\\subseteq\\{D\\}\\), so monotonicity of measure gives \\(\\mathbb P(C\\wedge D\\mid\\neg T)\\le\\min(\\alpha_C,\\alpha_D)\\) with no assumptions — the dual gate is never worse. Under conditional independence given \\(\\neg T\\) the conjunction factorizes, \\(\\mathbb P(C\\wedge D\\mid\\neg T)=\\alpha_C\\alpha_D\\), and \\(\\alpha_C\\alpha_D\\lt\\min(\\alpha_C,\\alpha_D)\\) iff \\(\\min\\gt 0\\) and \\(\\max\\lt 1\\) — a strict multiplicative drop. On the miss side, \\(\\mathbb P(\\neg(C\\wedge D)\\mid T)=\\mathbb P(\\neg C\\cup\\neg D\\mid T)\\le\\beta_C+\\beta_D\\) by the union bound: the price of demanding both is at worst additive in the individual miss rates while the benefit is multiplicative in the false-alarm rates — an asymmetric trade that favors the conjunction precisely when the signals are individually reliable on true completion (small \\(\\beta\\)) but individually gameable on non-completion (moderate \\(\\alpha\\)), which is the LLM self-assessment regime. Evidence form: if additionally \\(C\\perp D\\mid T\\), then \\(\\mathrm{odds}(\\neg T\\!:\\!T\\mid C\\wedge D)=\\dfrac{\\alpha_C\\,\\alpha_D}{(1-\\beta_C)(1-\\beta_D)}\\cdot\\mathrm{odds}(\\neg T\\!:\\!T)\\) — the log-Bayes-factors add, so a bar on the posterior that neither channel clears alone can be cleared conjunctively. Horizon necessity: a false stop at cycle \\(k\\) implies \\(G_k\\wedge\\neg T_k\\) for the gate event \\(G_k\\), so \\(\\mathbb P(\\text{any false stop})\\le\\sum_{k\\le N}\\mathbb P(G_k\\wedge\\neg T_k)\\le N\\max_k\\mathbb P(G_k\\mid\\neg T_k)\\): certifying a run-level budget \\(\\varepsilon\\) through this bound requires a per-cycle rate at most \\(\\varepsilon/N\\), which shrinks with the horizon — the dual gate's product is what supplies that headroom without requiring a better single verifier. Dependence, honestly: in general \\(\\mathbb P(C\\wedge D\\mid\\neg T)=\\kappa\\,\\alpha_C\\alpha_D\\) for some \\(\\kappa\\) in the Fréchet range \\(\\max(0,\\,\\alpha_C+\\alpha_D-1)/(\\alpha_C\\alpha_D)\\le\\kappa\\le 1/\\max(\\alpha_C,\\alpha_D)\\) (upper end by the min bound; the lower end, \\(\\kappa\\lt 1\\), is negative dependence and only helps). The failure mode is positive dependence, \\(\\kappa\\gt 1\\): \\(\\kappa\\) climbs toward its maximum exactly as the two signals collapse into one source — e.g. both computed from the model's own self-assessment — and at the top of the range the gate degrades to the better single signal, never below it. This is the mathematical content of requiring \\(D\\) to be <em>separately sourced</em>: separation is what drives \\(\\kappa\\) down to \\(1\\) and turns the min bound into the product.</p>\n<p><em>(ii) Sure termination, bounded blast radius, and inherited stall tests.</em> \\(\\tau\\le N\\) holds deterministically, which is stronger than almost-sure termination; total cost \\(\\sum_{k\\le\\tau}c_k\\le Nc_{\\max}\\) since the rate limit enforces \\(c_k\\le c_{\\max}\\); and with a checkpoint every \\(r\\) cycles, a detected runaway rolls back to a checkpoint at distance at most \\(r\\), bounding corruption by \\(r\\) cycles of mutations. These three are properties of the <em>enforced harness</em>, not of the agent's judgment — which is the point: they hold even when \\(q\\) and \\(p\\) are estimated badly. Stall test: under the contraction null, iterating the tower property gives \\(\\mathbb E[e_{k+w}\\mid\\mathcal F_k]\\le L^w e_k\\), and a tail (concentration) bound (\\(e\\ge 0\\); \\(e_k\\gt 0\\), else the verify condition already holds) yields \\(\\mathbb P(e_{k+w}\\ge e_k\\mid\\mathcal F_k)\\le L^w\\). At R8's moderate default \\(L\\approx\\tfrac12\\) with \\(w=3\\), a still-contracting loop shows a flat window with probability at most \\(1/8\\), while R8's no-guarantee regimes admit no such bound: the non-expansive isometry (\\(e_{k+1}=e_k\\)) fires the window with probability \\(1\\), and the martingale regime (\\(\\mathbb E[e_{k+1}\\mid\\mathcal F_k]=e_k\\)) contains the isometry as a special case, so no bound below \\(1\\) exists there. The window is therefore a proper hypothesis test: size at most \\(L^w\\) under the contraction null, decaying geometrically in \\(w\\), with power \\(1\\) against the stalled-at-a-fixed-distance alternative. Reflection test: by M4's lemma, \\(I(R_k;\\mathrm{cause}\\mid R_{\\lt k})=0\\) leaves the posterior over the cause unchanged; the next attempt, drawn by the same policy from that unchanged posterior, has the same success law, hence \\(p_{k+1}=p_k\\) — a reflection that repeats the prior cause with no new hypothesis cannot lift the hazard over a threshold it already failed, so the next cycle is the same losing bet: stop and escalate. Finally, in the good regime the cap is slack: contraction gives \\(\\mathbb E[e_k]\\le L^k e_0\\), so the expectation falls below \\(\\mathrm{tol}\\) within \\(\\lceil\\ln(e_0/\\mathrm{tol})/\\ln(1/L)\\rceil\\) cycles (R8's cap computation) and past that point a tail bound controls \\(\\mathbb P(e_k\\gt\\mathrm{tol})\\) by a geometrically decaying tail; a cap at that scale binds only on runs where the contraction hypothesis has already failed with high probability — the cap is insurance, not the mechanism.</p>\n<p><em>(iii) One threshold; exact optimality under the cap.</em> Absorption gives \\(\\mathbb P(\\neg T_{k+1}\\mid\\mathcal F_k)=\\mathbb P(\\neg T_k\\mid\\mathcal F_k)\\,\\mathbb P(\\neg T_{k+1}\\mid\\neg T_k,\\mathcal F_k)=(1-q_k)(1-p_{k+1})\\), and by the tower property \\(\\mathbb E[1-q_{k+1}\\mid\\mathcal F_k]\\) equals the same quantity. Stopping now and declaring done loses \\((1-q_k)W\\) in expectation; one more cycle and then stopping loses \\(c_{\\mathrm{cyc}}+(1-q_k)(1-p_{k+1})W\\). Continuing dominates iff \\(c_{\\mathrm{cyc}}\\lt(1-q_k)p_{k+1}W\\), i.e. \\[(1-q_k)\\,p_{k+1}\\ \\gt\\ \\frac{c_{\\mathrm{cyc}}}{W},\\] which is M4's threshold \\(p\\gt c_{\\mathrm{retry}}/V_{\\mathrm{fix}}\\) with the belief factor exposed: after a fresh observed failure M4 has \\(q_k=0\\) and the two coincide; at equality the two actions tie and the rule stops. At stop, declaring done loses \\((1-q_k)W\\) and escalating loses \\(c_{\\mathrm{esc}}\\), so declare done iff \\(q_k\\gt 1-c_{\\mathrm{esc}}/W\\) — a second expected-loss threshold, on \\(q_k\\) alone, and the dual gate is its implementation: by (i)'s additive log-evidence, \\(C_k\\wedge D_k\\) pushes \\(q_k\\) over the bar exactly when the combined Bayes factors suffice. (Folding escalation into the lookahead replaces the terminal loss by \\(\\min((1-q)W,\\,c_{\\mathrm{esc}})\\). Because \\(\\min\\) is concave the exact continue test becomes a value-of-information inequality, \\(\\mathbb E[\\min((1-q_{k+1})W,c_{\\mathrm{esc}})\\mid\\mathcal F_k]\\lt\\min((1-q_k)W,c_{\\mathrm{esc}})-c_{\\mathrm{cyc}}\\); under a posterior-resolving gate — the next gate reading collapses \\(q_{k+1}\\) into \\(\\{0,1\\}\\) — it evaluates in the escalate regime to \\(q_k+(1-q_k)p_{k+1}\\gt c_{\\mathrm{cyc}}/c_{\\mathrm{esc}}\\): the same threshold form with two changes, both exact for this loss. The bar rises from \\(c_{\\mathrm{cyc}}/W\\) to \\(c_{\\mathrm{cyc}}/c_{\\mathrm{esc}}\\) — strictly larger since \\(c_{\\mathrm{esc}}\\lt W\\): with escalation in hand only the recoverable stake rides on the outcome, so a cycle must earn more to be worth buying. And the left side gains the resolution term \\(q_k\\) — what a cycle now buys is the chance the gate <em>certifies</em> completion, an event of probability \\(q_k+(1-q_k)p_{k+1}\\) — so the folded rule is not uniformly stricter: it can rightly continue where the plain rule stops, when \\(q_k\\) is substantial but under the declare bar and one cycle suffices to resolve it. The form composes; only this one-step test, not first-crossing optimality, is claimed for the folded loss.) The two stop modes are the two collapses of the continue value \\((1-q_k)p_{k+1}\\): \\(q_k\\to 1\\) (gate fired: stop-done) or \\(p_{k+1}\\) stalled with \\(q_k\\) low (stop-escalate). \"Never spin\" is the statement that no third mode exists: below threshold, continuation is dominated. <br><em>Myopic \\(\\Rightarrow\\) optimal.</em> The cap makes the horizon finite, so the stopping problem is well posed with bounded losses. Let \\(\\ell_j=(1-q_j)W\\) and take the per-cycle cost constant at \\(c_{\\mathrm{cyc}}\\) (an \\(\\mathcal F_j\\)-measurable varying cost \\(c_j\\) only re-indexes the stop region by \\(c_j/W\\); the identical computation goes through). Define the one-step region \\(S_j=\\{(1-q_j)p_{j+1}\\le c_{\\mathrm{cyc}}/W\\}\\in\\mathcal F_j\\). For any stopping time \\(\\sigma\\) with \\(k\\le\\sigma\\le N\\), telescope the loss: \\(\\ell_\\sigma+(\\sigma-k)c_{\\mathrm{cyc}}=\\ell_k+\\sum_{j=k}^{\\sigma-1}Y_j\\) with \\(Y_j=\\ell_{j+1}-\\ell_j+c_{\\mathrm{cyc}}\\) and \\(\\mathbb E[Y_j\\mid\\mathcal F_j]=c_{\\mathrm{cyc}}-(1-q_j)p_{j+1}W\\ge 0\\) on \\(S_j\\). Under the monotone hypothesis — entering \\(S\\) means staying, \\(S_k\\subseteq S_j\\) a.s. for \\(j\\ge k\\); the diminishing-returns regime, of which M4's geometric hazard decay is a special case — every increment after entry is conditionally nonnegative, and since \\(\\{\\sigma\\gt j\\}\\in\\mathcal F_j\\), \\(\\mathbb E\\big[\\sum_{j}\\mathbf 1\\{\\sigma\\gt j\\}Y_j\\,\\big|\\,\\mathcal F_k\\big]=\\sum_j\\mathbb E\\big[\\mathbf 1\\{\\sigma\\gt j\\}\\,\\mathbb E[Y_j\\mid\\mathcal F_j]\\,\\big|\\,\\mathcal F_k\\big]\\ge 0\\) on \\(S_k\\). Hence every \\(\\sigma\\) does at least as badly as stopping at once on \\(S_k\\); off \\(S_k\\) the inequality \\((1-q_k)p_{k+1}\\gt c_{\\mathrm{cyc}}/W\\) is strict, so taking one more cycle strictly beats stopping: no optimal rule stops off \\(S\\) before the cap (at \\(j=N\\) the harness forces the stop wherever the process sits). First crossing of the threshold — or the cap, if the threshold is never crossed — is therefore an exact optimum: stopping off \\(S\\) before the cap is strictly dominated, and on \\(S\\) no continuation improves on stopping. <br><em>The cap's scale is principled.</em> Any rule \\(\\sigma\\) pays at least \\(\\sigma c_{\\min}\\), and the optimum's expected loss is at most that of stopping immediately, itself at most \\(W\\); hence \\(\\mathbb E[\\sigma^{\\ast}]\\,c_{\\min}\\le W\\), so \\(\\mathbb E[\\sigma^{\\ast}]\\le W/c_{\\min}\\) and \\(\\mathbb P(\\sigma^{\\ast}\\ge m\\,W/c_{\\min})\\le 1/m\\). The rational cycle budget is intrinsically bounded by the ratio of ship-incomplete loss to minimum cycle cost; a hard cap at scale \\(N\\approx W/c_{\\min}\\) uses this expectation as a budget heuristic, but does not establish a small truncation probability at that scale, while protecting against the risk the model cannot see from inside: a miscalibrated \\(\\hat q\\) or \\(\\hat p\\). \\(\\square\\)</p>\n<p><strong>In fabius.</strong> R12 wraps long-horizon autonomous work in the <code>step → verify</code> loop and exits only through the threshold: keep cycling while \\((1-q_k)p_{k+1}\\gt c_{\\mathrm{cyc}}/W\\); stop-done when the dual gate \\(C_k\\wedge D_k\\) — objective verify condition <em>plus</em> separately-sourced done-signal — pushes \\(q_k\\) over \\(1-c_{\\mathrm{esc}}/W\\); stop-escalate when a flat \\(w\\approx 3\\)-cycle progress window (R8's contraction refuted at test size \\(L^w\\)) or a cause-repeating reflection (M4's zero-information lemma) certifies the hazard has stalled below the bar; and the harness, not the model, enforces the cap \\(N\\), the rate limit \\(c_{\\max}\\), and checkpoints every \\(r\\) cycles.</p>\n<p><strong>Boundary.</strong> PROVED under stated hypotheses: the min bound, the additive miss bound, and the Fréchet range for \\(\\kappa\\) (assumption-free); the multiplicative false-stop drop and additive log-evidence (conditional independence of \\(C,D\\) given the truth); sure termination, cost \\(\\le Nc_{\\max}\\), and corruption \\(\\le r\\) cycles (given the harness enforces cap, rate limit, and checkpoints <em>externally</em> to the looping model); the stall test's size \\(\\le L^w\\) (under the stochastic-contraction null inherited from R8); the redundancy lemma (M4, verbatim, plus the stated same-policy premise); the threshold form (one-step lookahead); and exact optimality of the first-crossing-or-cap rule plus \\(\\mathbb E[\\sigma^{\\ast}]\\le W/c_{\\min}\\) (finite horizon + monotone hazard + constant per-cycle cost, for the declare-done terminal loss). OPERATIONAL, not proved: independence of \\(C\\) and \\(D\\) is a design target, not a theorem — the dependence factor \\(\\kappa\\) must be measured per deployment, and under full positive dependence the gate degrades, safely, to the better single signal; the stall test's \\(L^w\\) is a size bound only — its power is \\(1\\) against the stalled-at-fixed-distance alternative but unquantified against slow leaks hovering just inside the null; folding the escalate option in is established at one-step lookahead only — closed-form under a posterior-resolving gate, an open value-of-information inequality otherwise — and first-crossing optimality is proved for the declare-done loss alone, not the folded loss; the specific constants — window \\(w\\approx 3\\), the cap value, the checkpoint interval \\(r\\) — are heuristics for which the theory fixes only scales (\\(w\\) via the test size \\(L^w\\), \\(N\\) via \\(W/c_{\\min}\\), \\(r\\) via tolerable rework), not values; \\(q_k\\) and \\(p_{k+1}\\) are estimated by the very model that is looping, and a self-assessment-biased \\(\\hat q\\) is precisely the failure mode the separately-sourced \\(D\\) and the external cap exist to bound; and where the monotone hypothesis fails — fresh information genuinely revives the hazard — the first-crossing rule stops early and escalates when an oracle might have continued: the safe side of wrong, but not the optimal one.</p><p><strong>Implementation boundary.</strong> The posterior and hazard thresholds above describe the formal model. Loading a Markdown contract does not install calibrated estimators, independent completion signals, checkpoints, or external counters. The host or a concrete runner must supply and verify those controls before the termination and cost bounds apply.</p>"
 },
 {
  "rule": "R13",
  "title": "A vertical runs a studio — domain-led composition is conflict-free, terminating, and goal-preserving",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Collapse lemma (iv): the clause 'collapsing to any downstream layer admits outcomes violating g itself' was unqualified and is false when the domain block is unloaded (e.g. k≥2 via P and E loaded, A_D=Ω_D makes g=Ω unviolable; 'taking μ=D' requires μ loaded). Fixed by adding the explicit hypothesis A_D⊊Ω_D to the theorem statement and handling the degenerate case in the proof.",
   "Collapse lemma miscount: 'leaves all k−1≥1 other loaded blocks uncertified' undercounts when the dispatch layer λ is itself unloaded (then all k loaded blocks are uncertified). Fixed to 'at least k−1≥1, and all k if λ is unloaded'.",
   "Positivity overclaim: 'structurally positive whenever k≥2' (threshold paragraph) and 'the lemma proves the loss gap is positive' (Boundary c) overstate a set-theoretic lemma. Positivity of the expected-loss gap additionally needs zero studio miss-loss (follows from (i), now stated) and h_μ<1, L_μ>0 for some uncertified block (assumptions, now explicit in both places).",
   "Necessity counterexample silently assumed V_P=V_F=∅ so that K_P=K_F=Ω; without that, S*=K_E is unjustified. Made explicit.",
   "Boundary (b) noted locality compliance as operational but omitted the companion axiom that a layer's own-block constraint is satisfiable (A_λ≠∅); added, since vacuity of S* would otherwise be possible by a single layer's self-contradiction rather than only by locality violation."
  ],
  "html": "<p><strong>Statement.</strong> A vertical task (a game, a launch, a security review, a landing page) spans several concern-blocks at once. Model its concerns as a finite variable set \\(V\\) and lift R1's single-owner structure from tasks to concerns: an ownership map \\(\\omega:V\\to\\Lambda\\), \\(\\Lambda=\\{D,P,E,F\\}\\) (domain leader, disciplina, decor·cohors, parcus), assigns each concern exactly one owner. We prove that if every layer constrains only the block it owns, the studio composition \\(D\\prec P\\prec E\\prec F\\) is (i) well-defined and conflict-free, (ii) acyclic and terminating in one pass, and (iii) exactly goal-preserving — and, conversely, that collapsing a multi-block vertical to a single layer leaves every other loaded block uncertified, while a downstream locality violation can both redefine the domain goal and empty the pipeline. The routing gate is one threshold on one fresh variable: the loaded-block count \\(k(t)\\), with studio iff \\(k(t)\\ge 2\\).</p>\n<p><strong>Setting.</strong> Each concern \\(v\\in V\\) has a nonempty value domain \\(X_v\\); the outcome space is \\(\\Omega=\\prod_{v\\in V}X_v\\). By R1(i) applied to \\(\\omega\\) — preimages of disjoint singletons — the blocks \\(V_\\lambda=\\omega^{-1}(\\{\\lambda\\})\\) are pairwise disjoint and exhaust \\(V\\): a measurable partition of concern-space. Write \\(\\Omega_\\lambda=\\prod_{v\\in V_\\lambda}X_v\\) (a one-point space if \\(V_\\lambda=\\varnothing\\), by the empty-product convention) and \\(\\pi_\\lambda:\\Omega\\to\\Omega_\\lambda\\) for coordinate projection. <em>Locality axiom</em> (the single-owner discipline): layer \\(\\lambda\\) acts by publishing a constraint only on what it owns — a cylinder \\(K_\\lambda=\\pi_\\lambda^{-1}(A_\\lambda)\\) with \\(\\varnothing\\neq A_\\lambda\\subseteq\\Omega_\\lambda\\) — and operating on candidate sets \\(S\\subseteq\\Omega\\) as \\(T_\\lambda(S)=S\\cap K_\\lambda\\). The domain leader's constraint carries the goal: \\(g:=K_D\\). Call \\(\\lambda\\) <em>loaded</em> iff \\(A_\\lambda\\subsetneq\\Omega_\\lambda\\) (its constraint genuinely excludes something), and let \\(k(t)=|\\{\\lambda:\\lambda\\ \\text{loaded}\\}|\\).</p>\n<p><strong>Theorem (studio composition).</strong> Under single ownership and locality: (i) the composed output \\(S^{*}=(T_F\\circ T_E\\circ T_P\\circ T_D)(\\Omega)\\) equals \\(\\bigcap_{\\lambda}K_\\lambda\\), is independent of firing order, factorizes as \\(S^{*}\\cong\\prod_{\\lambda\\in\\Lambda}A_\\lambda\\), and is nonempty — in particular no two layers can publish contradictory constraints and no concern is legislated twice; (ii) the pipeline is a finite strict order, hence acyclic, and one topological pass reaches \\(S^{*}\\), a fixed point of every \\(T_\\lambda\\); (iii) \\(S^{*}\\subseteq g\\) and \\(\\pi_D(S^{*})=A_D\\): downstream layers refine strictly within their own coordinates and neither violate nor reshape the WHAT — and locality is necessary, since its failure admits both goal-redefinition and vacuity; (iv) (<em>collapse lemma</em>) if \\(k(t)\\ge 2\\) and the task is dispatched to a single layer \\(\\lambda\\), then for every other loaded \\(\\mu\\) the dispatch admits an outcome violating \\(K_\\mu\\); and when the domain block is itself loaded (\\(A_D\\subsetneq\\Omega_D\\) — a goal that excludes something, the defining mark of a vertical), collapsing to any downstream layer admits outcomes violating \\(g\\) itself.</p>\n<p><strong>Proof.</strong> <em>(i) Well-defined, conflict-free.</em> Because \\(\\{V_\\lambda\\}_{\\lambda\\in\\Lambda}\\) partitions \\(V\\) (totality of \\(\\omega\\) gives exhaustion, single-valuedness gives disjointness — R1's partition doing the structural work), coordinate evaluation \\(x\\mapsto(\\pi_\\lambda(x))_{\\lambda\\in\\Lambda}\\) is a bijection \\(\\Omega\\to\\prod_\\lambda\\Omega_\\lambda\\): every coordinate appears in exactly one factor. For \\(x\\in\\Omega\\): \\(x\\in\\bigcap_\\lambda K_\\lambda\\iff\\pi_\\lambda(x)\\in A_\\lambda\\ \\forall\\lambda\\iff(\\pi_\\lambda(x))_\\lambda\\in\\prod_\\lambda A_\\lambda\\), hence \\[ \\bigcap_{\\lambda\\in\\Lambda}K_\\lambda\\;\\cong\\;\\prod_{\\lambda\\in\\Lambda}A_\\lambda . \\] Intersection is commutative and associative, so every firing order yields the same \\(S^{*}=\\bigcap_\\lambda K_\\lambda\\). Each \\(A_\\lambda\\neq\\varnothing\\) and \\(\\Lambda\\) is finite, so choosing one \\(a_\\lambda\\in A_\\lambda\\) per layer (finite choice, no axiom needed) exhibits a point of \\(S^{*}\\): \\(S^{*}\\neq\\varnothing\\). The same construction for any pair \\(\\lambda\\neq\\mu\\) shows \\(K_\\lambda\\cap K_\\mu\\neq\\varnothing\\): cylinders over disjoint blocks cannot contradict each other, so an inter-owner conflict is structurally impossible, and each concern \\(v\\) is constrained exactly once — through \\(A_{\\omega(v)}\\), by its unique owner.</p>\n<p><em>(ii) Acyclic, terminating.</em> \\((\\Lambda,\\prec)\\) with \\(D\\prec P\\prec E\\prec F\\) is a strict total order on a finite set — an acyclic digraph with no back-edge — so the topological pass is its unique linear extension and fires each \\(T_\\lambda\\) exactly once: the pipeline halts after \\(|\\Lambda|=4\\) steps. Each \\(T_\\lambda\\) is intensive (\\(T_\\lambda(S)=S\\cap K_\\lambda\\subseteq S\\)) and idempotent (\\((S\\cap K_\\lambda)\\cap K_\\lambda=S\\cap K_\\lambda\\)), so \\(T_\\lambda(S^{*})=S^{*}\\) for every \\(\\lambda\\): re-firing any layer is a no-op, the descent in the complete lattice \\((2^{\\Omega},\\subseteq)\\) cannot oscillate, and the one-pass output is already the fixed point of the whole system.</p>\n<p><em>(iii) Objective preservation, and necessity of locality.</em> \\(S^{*}\\subseteq K_D=g\\), so every composed outcome satisfies the domain goal. For the projection: by (i), \\(S^{*}\\cong\\prod_\\lambda A_\\lambda\\) with every factor nonempty, and the projection of a product of nonempty sets onto one factor is that factor, so \\(\\pi_D(S^{*})=A_D\\) — downstream layers shrink nothing on \\(V_D\\): refinement, never redefinition. Necessity: let \\(V=\\{d,e\\}\\), \\(X_d=X_e=\\{0,1\\}\\), \\(V_D=\\{d\\}\\), \\(V_E=\\{e\\}\\) (so \\(V_P=V_F=\\varnothing\\): \\(A_P,A_F\\) are the one-point spaces, \\(K_P=K_F=\\Omega\\), and both drop out of the intersection), and let \\(E\\) break locality by publishing \\(K_E=\\{x:x_d=1\\}\\), a constraint on the domain's coordinate. If \\(A_D=\\{0,1\\}\\), then \\(S^{*}=K_E\\) and \\(\\pi_D(S^{*})=\\{1\\}\\subsetneq A_D\\): the execution layer has silently redefined the WHAT. If \\(A_D=\\{0\\}\\), then \\(S^{*}=K_D\\cap K_E=\\varnothing\\): the pipeline contradicts itself. So dropping locality falsifies conclusion (iii) already at two binary variables — both shipped prohibitions (\"don't collapse a vertical\", \"don't let a downstream layer redefine the goal\") are load-bearing axioms, not style.</p>\n<p><em>(iv) Collapse lemma.</em> Suppose \\(k(t)\\ge 2\\) and dispatch goes to the single layer \\(\\lambda\\); the dispatch certifies only membership in \\(K_\\lambda\\). Let \\(\\mu\\neq\\lambda\\) be any other loaded layer: \\(A_\\mu\\subsetneq\\Omega_\\mu\\) yields some \\(b\\in\\Omega_\\mu\\setminus A_\\mu\\). Choose \\(a\\in A_\\lambda\\) and, for every remaining block \\(\\nu\\), any \\(c_\\nu\\in\\Omega_\\nu\\) (all nonempty). The outcome \\(x\\) with \\(\\pi_\\lambda(x)=a\\), \\(\\pi_\\mu(x)=b\\), \\(\\pi_\\nu(x)=c_\\nu\\) lies in \\(K_\\lambda\\setminus K_\\mu\\): admitted by the collapsed dispatch, violating \\(\\mu\\)'s constraint. Hence a single-layer collapse leaves every other loaded block uncertified — at least \\(k-1\\ge 1\\) of them, and all \\(k\\) if \\(\\lambda\\) is itself unloaded. If moreover \\(D\\) is loaded, taking \\(\\mu=D\\) shows that collapsing to any downstream layer admits outcomes that violate the goal \\(g\\) itself; if \\(D\\) is unloaded, then \\(g=\\Omega\\) is unviolable but also contentless — there is no WHAT, and the task was never a vertical. \\(\\square\\)</p>\n<p><strong>In fabius (threshold form).</strong> R13's gate variable is the loaded-block count \\(k(t)\\in\\{0,\\dots,4\\}\\), computed from R1's load bits extended by a domain bit \\(\\ell_D\\) — a coordinate no other rule reads (R2 reads the rung, R3 the tool margin, R7 the width, M6–M8 the store). The gate is the single threshold \\[ \\text{run the studio}\\iff k(t)\\ \\ge\\ 2, \\] which implements the house expected-loss inequality \\(\\mathbb{E}[L\\mid\\text{collapse}]-\\mathbb{E}[L\\mid\\text{studio}]\\ \\gt\\ c_{\\text{pipe}}\\): the studio output \\(S^{*}\\) satisfies every \\(K_\\mu\\) by (i), so its constraint-miss loss is zero, while by the collapse lemma the collapse side carries the expected miss-cost \\(\\sum_{\\mu\\neq\\lambda,\\ \\mu\\ \\text{loaded}}(1-h_\\mu)L_\\mu\\) of the uncertified blocks (\\(h_\\mu\\) the chance an unconstrained block lands right anyway, \\(L_\\mu\\) its miss cost) — positive whenever \\(k\\ge 2\\) and some uncertified block has \\(h_\\mu\\lt 1\\) and \\(L_\\mu\\gt 0\\), the generic case for a genuinely loaded block. At the tie \\(k\\le 1\\) the rule takes the cheaper action — single dispatch, with parcus inline at \\(k=0\\) — matching R2/R3's cheap-at-tie convention, so R13 composes with the capstone's disjoint-variable argument as one more sign-consistent inequality in one more fresh variable. Inside each studio stage the downstream gates fire unchanged: R2 admits the rung, R6 \\(\\prec\\) R5 order execution, decor/cohors execute, parcus floors every stage.</p>\n<p><strong>Boundary.</strong> Proved: (i)–(iv) are theorems of the model — single ownership plus locality imply conflict-freeness, one-pass termination with a fixed point, exact goal preservation \\(\\pi_D(S^{*})=A_D\\), the impossibility of inter-owner contradiction, that any single-layer collapse admits outcomes violating every other loaded block's constraint (and the goal itself when the domain block is loaded), and (via the two-variable witnesses) the necessity of the locality axiom. Not proved, and stated as such: (a) <em>who leads</em> — the theorem takes \"the goal lives in \\(V_D\\)\" as given; that the domain layer, rather than process or execution, authors \\(g\\) is fabius's design choice, and since the \\(T_\\lambda\\) commute, the order \\(D\\prec P\\prec E\\prec F\\) matters operationally (who writes before whom reads) rather than set-theoretically; (b) <em>locality compliance</em> — that a real LLM layer constrains only its own block, and satisfiably so (\\(A_\\lambda\\neq\\varnothing\\)), is a prompt-level contract enforced operationally, not a provable property of the layer; the sharpness examples show exactly what breaks when it slips; (c) <em>the cap and the costs</em> — the threshold value \\(k\\ge 2\\) and the coordination cost \\(c_{\\text{pipe}}\\) are operational: the collapse lemma is set-theoretic — it proves violating outcomes are <em>admitted</em> — so the expected-loss gap is positive only under the mild extra assumptions \\(h_\\mu\\lt 1\\), \\(L_\\mu\\gt 0\\) on some uncertified block; the gap's magnitude \\(\\sum(1-h_\\mu)L_\\mu\\) versus \\(c_{\\text{pipe}}\\) is estimated in-context per task, not derived, and \\(k(t)\\) itself depends on the router's judgment of which blocks are genuinely loaded; (d) the model abstracts a layer's generative work to its constraint footprint \\(K_\\lambda\\) — content production is richer than the set it certifies.</p>"
 },
 {
  "rule": "M9",
  "title": "Externalization is lossless paging that wins once index recall clears an explicit threshold",
  "class": "real-math",
  "verdict": "corrected",
  "issues": [
   "Proof (a) chain 'i+s(t) \\le i+s_max \\le rB+s_max \\le W' asserts rB+s_max \\le W, which the slice-fits assumption (i+s_max \\le W) does not imply since i \\le rB points the wrong way; reordered so the W-bound attaches to actual occupancy only, with an explicit note that the majorant need not fit.",
   "Title and Statement claimed the win holds 'exactly' above rho* (an iff); the proof establishes only a one-sided sufficient condition — bundling is granted full-pool utility and Delta is an upper bound — so below rho* the theorem is silent except in the corollary-(ii) regime. Retitled, softened to 'whenever', and added an explicit ONE-SIDED clause to Theorem (c) and Boundary.",
   "Losslessness (b) tacitly assumed every dereference succeeds; capability invariance is now explicitly conditioned on store availability a=1, with a\\lt 1 priced in part (d), in Statement, Theorem, Proof, and Boundary.",
   "Delta's defining conditional expectation E[.|not E_t] is undefined at rho=1 (conditioning on a null event); added the vacuous-case convention (miss term is zero, any Delta \\ge 0 qualifies).",
   "The n=1 remark in Proof (a) needed c_1 \\gt 0 and the single page routed whole; entry lengths are now declared positive (|e_j|=c_j \\gt 0) in the Statement and the remark tightened.",
   "Proof (b) claimed the marginal capability is 'smaller by factor r \\ll 1' — r is a max over existing pages and need not bound the new page's ratio; replaced with the page's own catalog ratio c_{n+1}/l(p_{n+1}).",
   "Part (d)'s 'unconditionally positive at a=1 for any mu \\gt 0' fails for an empty corpus (B=0); now requires a nonempty corpus, with B \\gt 0 derived from l(p_j) \\gt 0.",
   "Statement's rho* \\ge 1 clause omitted corollary (ii)'s hypothesis B \\le W and overclaimed 'can never win' (a tie is possible at B=i+s-bar with rho=1); now reads 'fits the window' and 'never strictly win', and corollary (ii)'s gloss reads 'weakly dominates'.",
   "Statement promised 'three things' but the Theorem proves four parts and (d) was never summarized; count corrected to four and a one-line (d) summary added.",
   "Part (d): the claimed equality V_ext − V_bund-paged = μB − (1−a)Δ_a was false with Δ_a read as raw failure utility loss — on a store failure the external variant also avoids λ·s(t) residency for the unfilled slice, so 'all utility and λ terms cancel' fails on the failure event and the 'iff' breaks. Fixed by defining Δ_a in Theorem (d) as the NET conditional failure loss (utility forgone net of the residency the unfilled slice does not occupy) and rewriting the (d) proof to cancel terms only on the answered event, making the equality exact by construction; Statement (d) updated to match.",
   "Boundary: 'that second-order gap is absorbed into Δ by definition' was false — Δ is conditioned on miss events (complement of E_t), while the greedy-vs-exact-optimizer gap also opens on hit events, which Δ does not cover. Rewritten to state the theorem holds for the exact optimizer and the greedy implementation's hit-event gap must be measured or separately bounded.",
   "Edge case: r = max_j c_j/ℓ(p_j) is undefined for an empty corpus; added n ≥ 1 to the Statement.",
   "Theorem (a)/Proof (a): the limit (i+s_max)/B → r as B → ∞ silently held the corpus-dependent constant r fixed; corrected to 'at fixed r and s_max'.",
   "Proof (b): 'smaller by exactly that page's own catalog ratio' conflated 'smaller by' with 'equal to the fraction of'; corrected to 'equal to the fraction c_{n+1}/ℓ(p_{n+1}) of the bundled cost'.",
   "'In fabius.' asserted bulk corpora sit in the corollary-(i) regime as unmeasured fact, against the project's 'measured, not claimed' bar; rewritten so the corollary-(i) premise λ(B_r−i−s̄) ≥ Δ is explicitly checked by measurement before the unconditional move-out fires. Δ_a also added to the Boundary's list of estimated parameters.",
   "Verified sound and left unchanged: the (c) dominance chain and ρ* threshold (including Δ=0, ρ=1 null-event, and tie handling via 'some maximizer inside the slice'), both corollaries (corollary (ii) proof correctly holds for all ρ), i ≤ rB with r ≤ 1, the (b) bijection + Kraft/McMillan usage, the R9 greedy-transfer parenthetical (the covering analysis only needs OPT's elements available in the restricted pool), the n=1 no-free-win counterexample, and the absence of literal < / > in all 194 math segments."
  ],
  "html": "<p><strong>Statement.</strong> Let a skill's reference corpus be pages \\(C=\\{p_1,\\dots,p_n\\}\\), \\(n\\ge 1\\), with token lengths \\(\\ell(p_j)\\gt 0\\) and total \\(B=\\sum_{j}\\ell(p_j)\\). Packaging decides where \\(C\\) lives: <em>bundled</em> (shipped inside the artifact, resident on load) or <em>externalized</em> (the artifact ships only an index \\(I\\) with one entry \\(e_j\\) per page, \\(|e_j|=c_j\\gt 0\\), \\(i=\\sum_j c_j\\), and pages in one routed slice on demand via R9 retrieval over M7 addresses). We prove four claims and one corollary pair. (a) Paged context occupancy is \\(O(i+s)\\) versus bundled \\(\\Theta(\\min(B,W))\\), with the explicit constant \\(r=\\max_j c_j/\\ell(p_j)\\le 1\\). (b) Externalization is information-lossless: it is the \\(n\\)-fold composition of M7 evictions, a bijection on stored content, so — whenever the external store answers a dereference (the \\(a=1\\) idealization; part (d) prices \\(a\\lt 1\\)) — the reachable capability set is invariant and the marginal artifact cost of a new capability is one index entry \\(c_{n+1}\\), not one library \\(\\ell(p_{n+1})\\): \"adding a capability = adding an index entry\" is a theorem of the model, not a slogan. (c) In expected net utility, externalization dominates bundled residency whenever index recall clears an explicit threshold \\(\\rho^{*}\\) — a one-sided sufficient gate, since the comparison deliberately grants bundling full-pool utility; the two limits of that one inequality recover both clauses of the shipped rule — \\(\\rho^{*}\\le 0\\) for any corpus whose residency dwarfs one slice (externalize regardless of index quality), and \\(\\rho^{*}\\ge 1\\) when the corpus fits the window and is no larger than its own index plus one average slice (a lean entry doc or 50-line catalog stays inline; externalizing it can never strictly win). (d) Against bundled-but-paged (the library kept in the artifact but read through the same index), every answered-path term cancels, leaving install mass against availability: externalization wins iff \\((1-a)\\Delta_a\\lt\\mu B\\), with \\(\\Delta_a\\) the net failure loss defined in (d).</p>\n<p><strong>Setting.</strong> Reused objects. From R9: the budgeted-retrieval problem — candidate pool, lengths \\(\\mathrm{len}\\), monotone submodular relevance \\(f_t\\), retrieval budget \\(\\beta\\) (R9's budget, renamed to avoid collision with the library size \\(B\\)), and the cost-benefit greedy with seed enumeration achieving \\(1-1/e\\). From M7: the addressed slow tier — index entries are M7 addresses over an alphabet of size \\(D\\), uniquely decodable (distinct pages receive distinct, unambiguously parseable entries) and byte-gated, \\(c_j\\le\\ell(p_j)\\). New variables, disjoint from every other rule's as the coherence extension requires: window \\(W\\), with \\(i+s_{\\max}\\le W\\) (slices are cut to fit — that is what a slice is); task distribution \\(\\mathcal T\\); routing map \\(\\sigma\\) sending task \\(t\\) to slice \\(\\sigma(t)\\subseteq C\\) of size \\(s(t)=\\sum_{p\\in\\sigma(t)}\\ell(p)\\le s_{\\max}\\), mean \\(\\bar s=\\mathbb E_t[s(t)]\\); hit event \\(E_t\\): some budget-\\(\\beta\\)-feasible maximizer of \\(f_t\\) over \\(C\\) lies inside \\(\\sigma(t)\\); recall \\(\\rho=\\Pr_t[E_t]\\); task utility \\(u_t=g_t(f_t(S_t))\\) with \\(g_t\\) non-decreasing in retrieved relevance; residency price \\(\\lambda\\gt 0\\) utility per resident reference token (each displaces a task token in the fixed window; linear cost); miss bound \\(\\Delta\\ge\\mathbb E_t\\bigl[u^{\\mathrm{full}}_t-u^{\\mathrm{slice}}_t\\mid\\overline{E_t}\\bigr]\\), the conditional expected utility gap when routing misses (when \\(\\rho=1\\) the conditioning event is null: the requirement is vacuous, any \\(\\Delta\\ge 0\\) qualifies, and the miss term below is zero); install price \\(\\mu\\ge 0\\) per artifact token (the lean-stance cost the benchmark measures; set \\(\\mu=0\\) in part (c), which is therefore conservative, and isolated in part (d)). Access model: retrieval returns a budget-feasible \\(f_t\\)-optimal set from its pool (exact-optimizer idealization; R9's greedy supplies a uniform \\(1-1/e\\) implementation on both sides — see Boundary). Net values: \\(V_{\\mathrm{page}}=\\mathbb E[u^{\\mathrm{slice}}]-\\lambda(i+\\bar s)\\); \\(V_{\\mathrm{bund}}=\\mathbb E[u^{\\mathrm{res}}]-\\lambda B_r\\) with bundled residency \\(B_r=\\min(B,W)\\) and \\(u^{\\mathrm{res}}\\) the utility of retrieval over the resident portion.</p>\n<p><strong>Theorem.</strong> Under the Setting: <em>(a) Space.</em> For every task, paged occupancy obeys \\(i+s(t)\\le i+s_{\\max}\\le rB+s_{\\max}\\) with \\(r=\\max_j c_j/\\ell(p_j)\\le 1\\), versus bundled residency \\(B_r=\\min(B,W)\\); hence \\((i+s_{\\max})/B\\le r+s_{\\max}/B\\to r\\) as \\(B\\to\\infty\\) at fixed \\(r\\) and \\(s_{\\max}\\). <em>(b) Losslessness.</em> Externalization is a bijection between the bundled artifact and the pair (index in artifact, pages in external store): no stored information is created or destroyed, every capability reachable bundled is reachable externalized at the cost of exactly one dereference against a store that answers (\\(a=1\\); (d) prices \\(a\\lt 1\\)), artifact occupancy falls from \\(B\\) to \\(i\\), and capability \\(n{+}1\\) costs \\(c_{n+1}\\le\\ell(p_{n+1})\\) artifact tokens. <em>(c) Dominance threshold.</em> \\(V_{\\mathrm{page}}-V_{\\mathrm{bund}}\\ \\ge\\ \\lambda\\bigl(B_r-i-\\bar s\\bigr)-(1-\\rho)\\Delta\\), so externalization dominates whenever \\[\\rho\\ \\ge\\ \\rho^{*}\\ :=\\ 1-\\frac{\\lambda\\,(B_r-i-\\bar s)}{\\Delta}\\qquad(\\Delta\\gt 0),\\] and for \\(\\Delta=0\\) whenever \\(B_r\\ge i+\\bar s\\); the condition is sufficient, not necessary — below \\(\\rho^{*}\\) this one-sided bound is silent (see Boundary). Corollaries: (i) if \\(\\lambda(B_r-i-\\bar s)\\ge\\Delta\\) then \\(\\rho^{*}\\le 0\\) — externalize for every index, however bad; (ii) if \\(B\\le W\\) and \\(B\\le i+\\bar s\\) then \\(V_{\\mathrm{page}}-V_{\\mathrm{bund}}\\le 0\\) even at \\(\\rho=1\\) — the lean-entry-doc regime, where bundling weakly dominates even a perfect index. <em>(d) Install mass.</em> Against bundled-but-paged (library inside the artifact but read through the same index and selector), the read paths cancel term-by-term and \\(V_{\\mathrm{ext}}-V_{\\mathrm{bund\\text{-}paged}}=\\mu B-(1-a)\\Delta_a\\), where \\(a\\) is external-store availability and \\(\\Delta_a\\) is the <em>net</em> failure loss — the conditional expected drop in net value when a dereference goes unanswered, utility forgone net of the \\(\\lambda\\)-residency the unfilled slice no longer occupies (a definition, which is what makes the equality exact); externalization dominates iff \\((1-a)\\Delta_a\\lt\\mu B\\), and unconditionally at equal reliability \\(a=1\\) for any \\(\\mu\\gt 0\\) and nonempty corpus (\\(B\\gt 0\\)).</p>\n<p><strong>Proof.</strong> <em>(a)</em> The byte gate gives \\(c_j/\\ell(p_j)\\le 1\\) for each \\(j\\), so \\(r\\le 1\\) and \\(i=\\sum_j c_j\\le r\\sum_j\\ell(p_j)=rB\\). Paged occupancy is index plus routed slice: \\(i+s(t)\\le i+s_{\\max}\\le W\\) by the slice-fits assumption, and \\(i+s_{\\max}\\le rB+s_{\\max}\\) by \\(i\\le rB\\) (the majorant \\(rB+s_{\\max}\\) itself need not fit in \\(W\\); only the actual occupancy is assumed to). Bundled residency is \\(\\min(B,W)\\) by definition of residency in a window of size \\(W\\). Dividing by \\(B\\) gives the ratio bound; letting \\(B\\to\\infty\\) at fixed \\(r\\) and \\(s_{\\max}\\) sends it to \\(r\\). No win is claimed yet: for \\(n=1\\) with the single page routed whole, \\(i+s(t)=c_1+\\ell(p_1)\\gt\\ell(p_1)=B\\) since \\(c_1\\gt 0\\) — part (c) locates the crossover exactly. <em>(b)</em> Externalizing page \\(p_j\\) is literally M7's <code>write=EVICT</code> with slug \\(e_j\\): the index is an address book, so by hypothesis the entries form a uniquely decodable code, M7's necessity direction gives \\(\\sum_j D^{-c_j}\\le 1\\), the sufficiency direction certifies such a code coexists, and each dereference recovers \\(p_j\\) byte-for-byte whenever the store answers (unavailability is priced in (d), not here). By M7, each single eviction is a bijection \\((p_j\\ \\text{in artifact})\\leftrightarrow(e_j\\ \\text{in artifact},\\ p_j\\ \\text{in store})\\) changing artifact occupancy by \\(-(\\ell(p_j)-c_j)\\le 0\\). The \\(n\\) evictions act on disjoint pages with injective slugs, so their composition is a bijection between the bundled artifact and (index, store): information is relocated, never destroyed; occupancy drops from \\(B\\) to \\(i\\) by \\(\\sum_j(\\ell(p_j)-c_j)\\ge 0\\); and the marginal artifact cost of capability \\(n{+}1\\) is \\(c_{n+1}\\) versus \\(\\ell(p_{n+1})\\) — never larger, by the byte gate, and equal to the fraction \\(c_{n+1}/\\ell(p_{n+1})\\) of the bundled cost — far below \\(1\\) whenever pages dominate their catalog lines. <em>(c)</em> First a pointwise fact: \\(\\sigma(t)\\subseteq C\\), so the budget-feasible subsets of the slice are a subfamily of those of the corpus, the slice optimum of \\(f_t\\) is at most the corpus optimum, and since \\(g_t\\) is non-decreasing, \\(u^{\\mathrm{slice}}_t\\le u^{\\mathrm{full}}_t\\) for every \\(t\\); likewise the resident portion is a subset of \\(C\\), so \\(u^{\\mathrm{res}}_t\\le u^{\\mathrm{full}}_t\\) and \\(V_{\\mathrm{bund}}\\le\\mathbb E[u^{\\mathrm{full}}]-\\lambda B_r\\) (when \\(B\\gt W\\) this grant of full-pool utility to bundling is strictly generous, so the comparison is conservative — and one-sided; see Boundary). Now condition on routing. On \\(E_t\\) (probability \\(\\rho\\)): some unrestricted budget-feasible maximizer lies inside \\(\\sigma(t)\\), so the slice optimum equals the corpus optimum — the exact optimizer over the slice attains the same \\(f_t\\)-value, hence \\(u^{\\mathrm{slice}}_t=u^{\\mathrm{full}}_t\\). (This is where R9 transfers: its covering-inequality analysis quantifies only over feasible candidates and elements of the optimum, so with the optimum inside the slice the \\(1-1/e\\) guarantee holds verbatim on the restricted pool against the <em>unrestricted</em> optimum.) On \\(\\overline{E_t}\\) (probability \\(1-\\rho\\); if \\(\\rho=1\\) this event is null and the term vanishes): \\(\\mathbb E\\bigl[u^{\\mathrm{full}}-u^{\\mathrm{slice}}\\mid\\overline{E_t}\\bigr]\\le\\Delta\\) by definition of \\(\\Delta\\). Taking total expectations, \\(\\mathbb E[u^{\\mathrm{full}}]-\\mathbb E[u^{\\mathrm{slice}}]\\le(1-\\rho)\\Delta\\). Therefore \\(V_{\\mathrm{page}}-V_{\\mathrm{bund}}\\ge\\bigl(\\mathbb E[u^{\\mathrm{slice}}]-\\mathbb E[u^{\\mathrm{full}}]\\bigr)+\\lambda(B_r-i-\\bar s)\\ge\\lambda(B_r-i-\\bar s)-(1-\\rho)\\Delta\\). Non-negativity of the right side rearranges to \\((1-\\rho)\\Delta\\le\\lambda(B_r-i-\\bar s)\\), i.e. \\(\\rho\\ge\\rho^{*}=1-\\lambda(B_r-i-\\bar s)/\\Delta\\) for \\(\\Delta\\gt 0\\), and to \\(B_r\\ge i+\\bar s\\) for \\(\\Delta=0\\). This is one value-of-information gate: pay the expected miss loss \\((1-\\rho)\\Delta\\) to buy back \\(\\lambda(B_r-i-\\bar s)\\) of window. Corollary (i) is immediate: \\(\\lambda(B_r-i-\\bar s)\\ge\\Delta\\) forces \\(\\rho^{*}\\le 0\\). Corollary (ii): for \\(B\\le W\\) bundling is fully resident, so \\(u^{\\mathrm{res}}_t=u^{\\mathrm{full}}_t\\) exactly and \\(V_{\\mathrm{page}}-V_{\\mathrm{bund}}=\\bigl(\\mathbb E[u^{\\mathrm{slice}}]-\\mathbb E[u^{\\mathrm{full}}]\\bigr)+\\lambda(B-i-\\bar s)\\); the first term is \\(\\le 0\\) pointwise and the second is \\(\\le 0\\) when \\(B\\le i+\\bar s\\), so even a perfect index (\\(\\rho=1\\), first term zero) cannot make externalization strictly better. <em>(d)</em> If the library ships bundled but is read through the same index and the same selector, then on the event that the store answers (probability \\(a\\)) pools, hit events, recall, and occupancy \\(i+s(t)\\) are identical term-by-term, so every utility and \\(\\lambda\\) term cancels there; on the failure event (probability \\(1-a\\)) the entire conditional gap between the two variants — utility forgone <em>net of</em> the \\(\\lambda s(t)\\) residency the unfilled slice does not occupy — equals \\(\\Delta_a\\) by its definition in the Theorem (without the netting, the residency saved on failure would survive the subtraction and break the equality); and the artifacts differ in install mass by exactly \\(B\\) beyond the shared index. Hence \\(V_{\\mathrm{ext}}-V_{\\mathrm{bund\\text{-}paged}}=\\mu B-(1-a)\\Delta_a\\) exactly: positive iff \\((1-a)\\Delta_a\\lt\\mu B\\) — the same threshold form as (c) with \\((a,\\Delta_a,\\mu B)\\) in place of \\((\\rho,\\Delta,\\lambda(B_r-i-\\bar s))\\) — and, at \\(a=1\\), positive for any \\(\\mu\\gt 0\\) since \\(B=\\sum_j\\ell(p_j)\\gt 0\\) for a nonempty corpus. \\(\\square\\)</p>\n<p><strong>In fabius.</strong> M9 is the R9/M7 paging gate applied to the system's own packaging: every skill ships a lean entry doc plus index entries (M7 slugs) and pages in one routed slice through R9's density greedy; a <code>references/</code> folder that grows past a lean entry doc is tested against \\(\\rho\\ge\\rho^{*}\\). Bulk corpora — agent catalogs, design teardowns, swipe files, hardening guides, vector stores — carry residency far beyond one slice, so the corollary-(i) premise \\(\\lambda(B_r-i-\\bar s)\\ge\\Delta\\) is checked by measurement (per the Boundary) and, once it holds, \\(\\rho^{*}\\le 0\\): they move out regardless of index quality; a genuine 50-line catalog sits in corollary (ii) and stays inline. Adding a capability adds \\(c_{n+1}\\) tokens of index, not \\(\\ell(p_{n+1})\\) of library.</p>\n<p><strong>Boundary.</strong> PROVED: the occupancy bound (a) with the byte-gate constant \\(r\\le 1\\); losslessness (b) as a composition of M7 bijections, inheriting M7's prefix-code machinery wholesale, conditional on the store answering dereferences — availability is not free, it is the \\(a\\) that (d) prices; the dominance threshold \\(\\rho^{*}\\) and both corollaries in (c), under three declared modeling assumptions — linear residency cost \\(\\lambda\\) per resident token, \\(\\Delta\\) defined as a bound on the conditional miss gap, and the exact-optimizer access model; and the install-mass comparison (d) with its availability threshold. ONE-SIDED: the (c) gate is sufficient, never necessary — bundling is granted full-pool utility and \\(\\Delta\\) is only an upper bound, so for \\(\\rho\\lt\\rho^{*}\\) the theorem is silent (except in the corollary-(ii) regime, where bundling provably weakly dominates); close calls below the gate are decided by measurement, not by this theorem. OPERATIONAL, not proved: every numerical value — \\(\\rho\\) must be measured per index as miss-rate over a task log, and \\(\\lambda,\\Delta,\\Delta_a,\\mu,a\\) estimated, never assumed; the linearity of \\(\\lambda\\) (real long-context degradation is plausibly convex, which would only widen externalization's win at large \\(B_r\\), but that is a conjecture, not a theorem here); the exact-optimizer idealization — under R9's greedy the two pools share the same benchmark optimum on hit events and greedy earns the same \\(1-1/e\\) guarantee against it on either pool, but pointwise selections may differ inside the band, opening a second-order hit-event gap that \\(\\Delta\\) (a miss-conditional bound) does not cover: the theorem as proved holds for the exact optimizer, and the greedy implementation needs that gap measured or separately bounded; and the migration inventory itself — which libraries still ship bundled under <code>references/</code>, and the count — is a measured fact owned by CORPUS.md, not a consequence of this theorem.</p>"
 }
]
